# Matrice de couverture FME→GeoStudio

289 transformers FME recensés.

## Résumé par statut

| Statut | Nombre |
|---|---|
| `implemented` | 87 |
| `license_blocked` | 10 |
| `out_of_scope` | 25 |
| `planned_duckdb` | 60 |
| `planned_gdal` | 55 |
| `planned_otb` | 3 |
| `planned_pdal` | 16 |
| `planned_rust` | 2 |
| `qgis_frozen` | 19 |
| `unknown` | 12 |

## Résumé par moteur

| Moteur | Nombre |
|---|---|
| `duckdb` | 147 |
| `gdal` | 59 |
| `n/a` | 42 |
| `otb` | 3 |
| `pdal` | 17 |
| `qgis` | 19 |
| `rust:encoding_rs` | 2 |

## Détail

| Transformer FME | Catégorie | Équivalent GeoStudio | Moteur | Licence | Statut | Fréquence | Notes |
|---|---|---|---|---|---|---|---|
| AggregateFilter | Attribute |  | duckdb |  | `planned_duckdb` | niche | Déjà atteignable aujourd'hui comme transform.aggregate (métrique par groupe) → transform.filter (filtre HAVING-style en aval sur cette métrique) — même patron de composition que DatabaseJoiner, mais ce n'est pas un op unique existant. Reclassé depuis implemented (composition de 2 op ≠ match 1:1 exigé par la méthode de catégorisation). |
| AttributeCopier | Attribute | transform.derive | duckdb |  | `implemented` | courant | SELECT col AS new_col — expression SQL triviale à une colonne. |
| AttributeCreator | Attribute | transform.derive | duckdb |  | `implemented` | courant | Ajout d'un attribut à valeur fixe ou calculée : cas d'usage direct de transform.derive (colonne + expression SQL bornée). |
| AttributeFilter | Attribute | transform.filter | duckdb |  | `implemented` | courant | Le test par attribut est un transform.filter classique ; le routage multi-port se traduit par plusieurs nœuds transform.filter branchés sur la même source dans le canevas DAG, pas une capacité nouvelle. |
| AttributeKeeper | Attribute | transform.select | duckdb |  | `implemented` | courant | Même op que AttributeRemover, usage inverse (liste blanche plutôt que liste noire) — transform.select couvre les deux sens. |
| AttributeManager | Attribute |  | duckdb |  | `planned_duckdb` | courant | Déjà atteignable aujourd'hui comme une composition de 2 nœuds sur le canevas (transform.select pour le renommage/suppression/réordonnancement, transform.derive pour le calcul de valeurs), une topologie déjà supportée — mais ce n'est pas un op unique existant. Reclassé depuis implemented (composition de 2 op ≠ match 1:1 exigé par la méthode de catégorisation) — un futur op unique combinant les deux resterait à construire pour un vrai match 1:1. |
| AttributeRangeMapper | Attribute | transform.derive | duckdb |  | `implemented` | courant | CASE WHEN col BETWEEN a AND b THEN v1 ... ELSE défaut END — expression bornée classique de reclassement, même patron que la symbologie par classes (SP-25). |
| AttributeRemover | Attribute | transform.select | duckdb |  | `implemented` | courant | transform.select accepte un mapping colonne→null pour omettre une colonne du flux de sortie. |
| AttributeRenamer | Attribute | transform.select | duckdb |  | `implemented` | courant | transform.select prend un dict colonne→nouveau nom, correspondance directe. |
| AttributeSplitter | Attribute | transform.derive | duckdb |  | `implemented` | niche | string_split(value, delimiter) — fonction de découpage confirmée dans la documentation DuckDB, expression bornée à une ligne produisant une valeur de type LIST, même patron que ListBuilder. Non vérifié : si writer.collection peut effectivement persister la colonne LIST résultante — core/app/collections/introspection.py::FieldType n'a aujourd'hui aucun type liste/tableau. Le calcul lui-même fonctionne via transform.derive, mais l'écriture du résultat dans une collection pourrait ne pas être supportée. Rigueur symétrique de ListBuilder plutôt qu'omission (le premier jet de cette tâche avait omis ce transformer par prudence sur ce même point, cf. task-8-report.md). |
| AttributeValidator | Attribute | transform.validateAttributes | duckdb |  | `implemented` | niche | Le routage multi-port sur plusieurs critères combinés PLUS la génération d'un rapport de synthèse dépasse une simple expression booléenne de transform.filter ; un test d'unicité nécessite en outre une comparaison inter-lignes (fenêtre/groupBy), hors contrat d'expression bornée à une ligne. |
| AttributeValueMapper | Attribute | transform.derive | duckdb |  | `implemented` | courant | CASE WHEN col = 'a' THEN 'A' WHEN col = 'b' THEN 'B' ELSE col END — table de correspondance statique connue à l'écriture du pipeline, expression bornée. |
| BulkAttributeRemover | Attribute | transform.bulkRemoveAttributes | duckdb |  | `implemented` | niche | transform.select exige une liste de colonnes explicite à la sauvegarde de la config ; une suppression par motif sur un schéma inconnu à l'avance nécessite une introspection dynamique du schéma au moment de l'exécution, absente du contrat actuel de l'op. |
| BulkAttributeRenamer | Attribute | transform.bulkRenameAttributes | duckdb |  | `implemented` | niche | Même limite que BulkAttributeRemover : renommage par motif sur un schéma dynamique, au-delà du dict statique colonne→nom de transform.select. |
| ChangeDetector | Attribute | transform.detectChanges | duckdb |  | `implemented` | courant | La comparaison AUTOMATIQUE de tous les attributs communs suppose une introspection dynamique du schéma des deux flux (colonnes inconnues à l'avance) ; transform.join ne supporte que inner/left (pas de full outer, nécessaire pour détecter les suppressions), et transform.derive ne peut pas énumérer dynamiquement des colonnes non nommées à l'écriture du pipeline. |
| Counter | Attribute | transform.derive | duckdb |  | `implemented` | courant | À ne pas confondre avec un besoin de comptage spatial (transform.countWithin) : Counter compte des features dans leur ordre d'arrivée, sans aucune relation spatiale — confirmé contre la documentation FME (« won't consider any spatial relationships »). Le cas d'usage réel (numéro de séquence, éventuellement par groupe) est une expression de fenêtrage SQL : `ROW_NUMBER() OVER (PARTITION BY <clés de groupe> ORDER BY <col>)`, expression bornée à une colonne exécutable par transform.derive (DuckDB supporte les fonctions fenêtrées dans une clause SELECT sans GROUP BY). La portée « Globale » (compteur partagé entre plusieurs transformers distincts du même workspace FME) n'a pas d'équivalent : chaque nœud transform.derive du DAG GeoStudio est indépendant. |
| DatabaseJoiner | Attribute |  | duckdb |  | `planned_duckdb` | courant | Déjà atteignable aujourd'hui comme reader.connector.postgres (lecture de la table externe) → transform.join (jointure par clé), une topologie à 2 prédécesseurs déjà supportée par core/app/pipelines/compiler.py — mais ce n'est pas un op unique existant. Reclassé depuis implemented (composition de 2 op ≠ match 1:1 exigé par la méthode de catégorisation). |
| DuplicateFilter | Attribute | transform.aggregate | duckdb |  | `implemented` | courant | groupBy sur les attributs clés + métrique first()/any_value() par colonne restante = exactement une ligne par groupe, patron SQL standard de déduplication. |
| Matcher | Attribute |  | duckdb |  | `planned_duckdb` | niche | Le marquage (plutôt que la suppression) de doublons nécessite de compter les occurrences par groupe puis de reporter ce compte sur CHAQUE ligne d'origine — une fonction fenêtrée (COUNT() OVER PARTITION BY), hors du contrat d'expression scalaire à une ligne de transform.derive ; la comparaison géométrique optionnelle ajoute un prédicat spatial inter-lignes non exposé par les op actuels. |
| NullAttributeMapper | Attribute | transform.derive | duckdb |  | `implemented` | courant | COALESCE(col, valeur) ou CASE WHEN col = valeur THEN NULL ELSE col END — expression SQL bornée à une colonne. |
| StatisticsCalculator | Attribute | transform.aggregate | duckdb |  | `implemented` | courant | Correspondance directe : `groupBy` + `metrics` (expressions SQL MIN/MAX/AVG/STDDEV/SUM/COUNT) de transform.aggregate reproduisent exactement ce cas d'usage — une ligne de sortie par groupe (ou une seule si `groupBy` est vide), fonctions d'agrégation standard confirmées côté DuckDB. Correspondance plus directe encore que celle établie pour Aggregator dans ce même lot (même op transform.aggregate, cas d'usage complémentaire — précédent déjà établi par ce fichier pour plusieurs transformers distincts partageant un même op, ex. les 5 *Reprojector). |
| AttributeReprojector | Coordinate System | transform.reprojectAttribute | duckdb |  | `implemented` | niche | Implémenté : transform.reprojectAttribute reprojette deux colonnes attribut x/y (pas la géométrie portée par la feature). |
| CoordinateConcatenator | Coordinate System | transform.concatCoordinates | duckdb |  | `implemented` | niche | Implémenté : transform.concatCoordinates construit une géométrie point à partir de deux colonnes attribut X/Y ; ne nécessite pas de colonne geometry pré-existante en entrée (bug corrigé lors de la revue finale de vague 1). |
| CoordinateExtractor | Coordinate System | transform.extractCoordinates | duckdb |  | `implemented` | niche | Implémenté : transform.extractCoordinates écrit les coordonnées X/Y de la géométrie dans des attributs. |
| CoordinateRounder | Coordinate System | transform.roundCoordinates | duckdb |  | `implemented` | niche | Implémenté : transform.roundCoordinates réduit la précision des coordonnées par taille de grille (gridSize), pas par un nombre de décimales. |
| CoordinateSwapper | Coordinate System | transform.swapCoordinates | duckdb |  | `implemented` | niche | Implémenté : transform.swapCoordinates permute les axes X/Y de la géométrie. |
| CoordinateSystemDescriptionConverter | Coordinate System |  | gdal | MIT/X (GDAL/OGR) | `planned_gdal` | niche | Conversion entre représentations de CRS (WKT/EPSG/PROJ4/PROJJSON) — territoire classique de la bibliothèque OSR de GDAL, aucun op pipeline GeoStudio actuel ne l'expose. |
| CoordinateSystemExtractor | Coordinate System | transform.extractSrid | duckdb |  | `implemented` | niche | Implémenté : transform.extractSrid lit le SRID porté par le runtime du pipeline et l'écrit dans un attribut. |
| CoordinateSystemRemover | Coordinate System |  | duckdb |  | `planned_duckdb` | niche | Pas de cible cohérente dans ce runtime : le SRID est un entier obligatoire porté hors-bande par core/app/pipelines/runtime.py, jamais stocké sur la géométrie elle-même (aucune fonction ST_SetSRID/ST_SRID dans l'extension spatiale DuckDB chargée, vérifié empiriquement — vague 1 transformers DuckDB §0, 2026-09-17/18). transform.setSrid (vague 1) couvre uniquement CoordinateSystemSetter (réassignation). Retirer un SRID n'a pas de sens ici : ne pourra être couvert que si une notion de « SRID inconnu/absent » est introduite dans le modèle de pipeline — chantier séparé, non entamé. |
| CoordinateSystemSetter | Coordinate System | transform.setSrid | duckdb |  | `implemented` | niche | Implémenté : transform.setSrid réassigne le SRID de la géométrie sans reprojeter les coordonnées. |
| CsmapReprojector | Coordinate System | transform.reproject | duckdb |  | `implemented` | niche | Même besoin fonctionnel que Reprojector (changement de CRS) exposé sous un nom de transformer distinct par FME selon le moteur géodésique choisi — couvert par le même op transform.reproject. |
| DMSCalculator | Coordinate System | transform.formatCoordinates | duckdb |  | `implemented` | niche | Implémenté : transform.formatCoordinates (mode dms) retourne une chaîne de texte au format degrés/minutes/secondes, sans indicateur d'hémisphère (N/S/E/O). |
| DecimalDegreesCalculator | Coordinate System | transform.formatCoordinates | duckdb |  | `implemented` | niche | Implémenté : transform.formatCoordinates (mode decimalDegrees) arrondit la coordonnée — retourne une valeur NUMÉRIQUE arrondie, pas une chaîne de texte. |
| DimensionExtractor | Coordinate System | transform.extractDimension | duckdb |  | `implemented` | niche | Implémenté : transform.extractDimension écrit la dimension de coordonnées (2 ou 3) de la géométrie, pas sa dimension topologique (point/ligne/surface). |
| ElevationExtractor | Coordinate System | transform.extractElevation | duckdb |  | `implemented` | niche | Implémenté : transform.extractElevation écrit la coordonnée Z dans un attribut, NULL si la géométrie n'a pas de Z. |
| EsriReprojector | Coordinate System | transform.reproject | duckdb |  | `implemented` | niche | Même besoin fonctionnel que Reprojector, variante utilisant le moteur géodésique Esri plutôt que CS-Map/PROJ — la sélection fine de transformation géodésique (datum shift) au-delà du CRS cible n'est pas exposée par transform.reproject (`targetCrs` seul). |
| GtransAttributeReprojector | Coordinate System | transform.reprojectAttribute | duckdb |  | `implemented` | niche | Implémenté : transform.reprojectAttribute couvre ce besoin (reprojection de deux colonnes attribut x/y, pas la géométrie) — même opération que AttributeReprojector, moteur géodésique différent (Gtrans) sans distinction côté GeoStudio. |
| GtransReprojector | Coordinate System | transform.reproject | duckdb |  | `implemented` | niche | Même besoin fonctionnel que Reprojector — la disponibilité effective de la grille de transformation nationale spécifique à Gtrans dans la distribution PROJ utilisée par GeoStudio n'a pas été vérifiée en session, nuance documentée plutôt que supposée réglée. |
| LatLongToMGRSConverter | Coordinate System |  | n/a |  | `unknown` | niche | Aucune crate Rust MGRS confirmée mature en recherche limitée à cette tâche, et GDAL/PROJ n'exposent pas cette conversion nativement — à retrancher lors d'un futur passage dédié plutôt que deviner un moteur. |
| LocalCoordinateSystemSetter | Coordinate System |  | n/a |  | `unknown` | niche | Systèmes de coordonnées locaux/arbitraires (hors registre EPSG standard) — aucun moteur candidat clair identifié en recherche limitée à cette tâche, à retrancher lors d'un futur passage dédié plutôt que deviner. |
| MGRSToLatLongConverter | Coordinate System |  | n/a |  | `unknown` | niche | Même limitation que LatLongToMGRSConverter : aucun moteur candidat mature confirmé en recherche limitée à cette tâche. |
| PROJAttributeReprojector | Coordinate System | transform.reprojectAttribute | duckdb |  | `implemented` | niche | Implémenté : transform.reprojectAttribute couvre ce besoin (reprojection de deux colonnes attribut x/y, pas la géométrie) — même opération que AttributeReprojector, moteur géodésique différent (PROJ) sans distinction côté GeoStudio. |
| PROJReprojector | Coordinate System | transform.reproject | duckdb |  | `implemented` | niche | Même besoin fonctionnel que Reprojector — PROJ est d'ailleurs la bibliothèque de reprojection sous-jacente typique de l'écosystème GDAL/PostGIS/DuckDB spatial, cohérent avec transform.reproject. |
| RasterGCPExtractor | Coordinate System |  | gdal | MIT/X (GDAL/OGR) | `planned_gdal` | niche | Opération inverse de RasterGCPSetter — même bucket moteur candidat (GDAL, métadonnées GCP raster). |
| RasterGCPSetter | Coordinate System |  | gdal | MIT/X (GDAL/OGR) | `planned_gdal` | niche | Géoréférencement raster par GCP (territoire classique gdal_translate -gcp/gdalwarp -tps) — aucun op pipeline GeoStudio actuel ne l'expose. |
| RasterGeoreferencer | Coordinate System |  | gdal | MIT/X (GDAL/OGR) | `planned_gdal` | niche | Géoréférencement raster par paramètres géométriques (territoire classique gdal_edit.py/gdal_translate -a_ullr) — aucun op pipeline GeoStudio actuel ne traite de raster brut non géoréférencé. |
| ReframeReprojector | Coordinate System | transform.reproject | duckdb |  | `implemented` | niche | Même besoin fonctionnel que Reprojector — la disponibilité effective des grilles de transformation Réframe (swisstopo) dans la distribution PROJ utilisée par GeoStudio n'a pas été vérifiée en session, nuance documentée plutôt que supposée réglée. |
| Reprojector | Coordinate System | transform.reproject | duckdb |  | `implemented` | courant | Correspondance directe avec transform.reproject (op pipeline générique de changement de CRS, `targetCrs`). |
| VertexCounter | Coordinate System | transform.countVertices | duckdb |  | `implemented` | niche | Implémenté : transform.countVertices écrit le nombre de sommets de la géométrie dans un attribut. |
| DatabaseDeleter | Database |  | duckdb | MIT (DuckDB) + Apache-2.0 (dlt, destinations SQL en écriture) | `planned_duckdb` | niche | Même lacune que DatabaseUpdater (pas de writer vers une base externe) — cas d'usage plus étroit (suppression seule). |
| DatabaseQuerier | Database |  | duckdb | MIT (DuckDB) + Apache-2.0 (dlt) | `planned_duckdb` | courant | Distinct de reader.connector.postgres/snowflake (spécifiques à un moteur) : DatabaseQuerier est générique sur le type de SGBD via les connexions nommées FME — étendre l'architecture reader.connector.* à un pilote SQLAlchemy configurable par tenant couvrirait ce besoin sans nouveau moteur d'exécution. |
| DatabaseUpdater | Database |  | duckdb | MIT (DuckDB) + Apache-2.0 (dlt, destinations SQL en écriture) | `planned_duckdb` | courant | Aucun des 3 connecteurs actuels (reader.connector.*) n'écrit vers une base externe — seuls writer.collection/export/dataset écrivent dans le cœur GeoStudio lui-même. Un futur writer.connector.postgres (dlt destination SQL) serait le pendant symétrique côté écriture. |
| ExcelConditionalFormatter | Database |  | n/a |  | `out_of_scope` | niche | Mise en forme cosmétique d'un tableur, sans rapport avec un moteur de traitement de données géospatiales/analytiques — writer.export (XLSX) de GeoStudio produit des données brutes, jamais de styles de cellule. |
| ExcelStyler | Database |  | n/a |  | `out_of_scope` | niche | Même famille qu'ExcelConditionalFormatter — mise en forme cosmétique, aucun rapport avec un moteur de traitement de données. |
| FeatureReader | Database |  | n/a |  | `out_of_scope` | niche | Capacité d'orchestration absente de l'architecture GeoStudio, pas un manque de moteur : les readers d'un pipeline sont des nœuds sources fixés à la conception (core/app/pipelines/runtime.py compile un DAG unique), aucun n'est invocable dynamiquement au milieu d'un traitement selon un attribut — même limitation de fond que WorkspaceRunner (déjà dans la matrice, Workflow/Logic). |
| FeatureWriter | Database |  | n/a |  | `out_of_scope` | niche | Symétrique de FeatureReader — même limitation d'architecture (pas d'écriture dynamique mi-pipeline), même rapprochement avec WorkspaceRunner. |
| InlineQuerier | Database |  | duckdb | MIT (DuckDB) | `planned_duckdb` | niche | Tout pipeline GeoStudio se compile déjà intégralement vers du SQL DuckDB (core/app/pipelines/compiler.py) — un futur transform.sql exposant un fragment SQL brut comme étape serait une extension naturelle du moteur déjà en place, pas un nouveau moteur à intégrer ; distinct des 19 ops actuels, qui n'exposent aucune échappatoire SQL libre à l'utilisateur. |
| SQLCreator | Database |  | duckdb | MIT (DuckDB) + Apache-2.0 (dlt) | `planned_duckdb` | niche | Recoupe DatabaseQuerier/SQLExecutor — même lacune d'architecture (connexion nommée générique multi-SGBD), pas un besoin distinct. |
| SQLExecutor | Database |  | duckdb | MIT (DuckDB) + Apache-2.0 (dlt) | `planned_duckdb` | niche | Même famille que HTTPCaller (déjà dans la matrice, Workflow/Logic) : un appel par ligne au milieu du pipeline plutôt qu'une lecture unique en tête de DAG — capacité différente du patron reader.connector.* actuel (lecture de tête), pas seulement un nouveau connecteur. |
| AttributeRangeFilter | Filtering & Routing |  | duckdb |  | `planned_duckdb` | niche | Chaque plage configurée est individuellement un WHERE trivial pour transform.filter, et le fan-out d'un nœud par plage — branchés sur le même prédécesseur — est déjà supporté par la topologie du DAG (core/app/pipelines/compiler.py::predecessor_id ne borne que les arêtes ENTRANTES d'un nœud, jamais ses arêtes sortantes). La vraie limite : les plages numériques sont définies par l'utilisateur et peuvent se chevaucher, et FME résout un chevauchement par priorité du premier match dans l'ordre de la table de bornes — même problème d'ordre/exclusivité que TestFilter, pas un problème de topologie. |
| ClosedCurveFilter | Filtering & Routing | transform.filter | duckdb |  | `implemented` | niche | ST_IsClosed(geometry) — fonction confirmée dans la doc DuckDB spatial. Couvre le test binaire Closed/Open (un port par nœud, patron Tester) ; le port <Rejected> (features qui ne sont pas des courbes) n'a pas d'équivalent, non vérifié indépendamment côté DuckDB. |
| ConvexityFilter | Filtering & Routing | transform.filter | duckdb |  | `implemented` | niche | ST_Area(geometry) >= ST_Area(ST_ConvexHull(geometry)) * 0.999 (tolérance flottante) — ST_Area et ST_ConvexHull tous deux confirmés dans la doc DuckDB spatial, expression scalaire sans référence de table donc valide pour transform.filter. Port Undefined (lignes, points, arcs, rasters, texte — classés Undefined et non Concave par ConvexityFilter) non couvert par cette seule expression. |
| GeometryFilter | Filtering & Routing |  | duckdb |  | `planned_duckdb` | niche | Chaque catégorie de géométrie est un test ST_GeometryType(geometry) trivial pour transform.filter pris isolément (fonction confirmée dans la doc DuckDB spatial, github.com/duckdb/duckdb-spatial/blob/main/docs/functions.md). Le mode simple (catégories mutuellement exclusives : point/ligne/surface/agrégat/etc.) est structurellement identique à MultipleGeometryFilter/ClosedCurveFilter (implemented) — fan-out de N nœuds transform.filter au même op branchés sur le même prédécesseur, capacité déjà supportée par la topologie du DAG (core/app/pipelines/compiler.py::predecessor_id ne borne que les arêtes ENTRANTES d'un nœud, jamais ses arêtes sortantes) : pris isolément, ce mode serait implemented. Le mode détaillé introduit des catégories qui peuvent se chevaucher avec une priorité d'affectation de port — même problème d'ordre/exclusivité que TestFilter, qu'aucun outil de ce dépôt ne vérifie entre nœuds transform.filter indépendants. Le transformer représentant les deux modes sous une seule ligne de la matrice, coverage_status reste planned_duckdb au compte du mode détaillé (lecture conservatrice) ; ce n'est pas une limite de fan-out mais bien la même limite d'ordre/priorité que TestFilter. |
| MultipleGeometryFilter | Filtering & Routing | transform.filter | duckdb |  | `implemented` | niche | ST_NumGeometries(geometry) > 1 — fonction confirmée dans la doc DuckDB spatial (github.com/duckdb/duckdb-spatial/blob/main/docs/functions.md). Test binaire à un seul port matérialisé par nœud, même patron que Tester. Non couvert : le paramètre avancé « Treat Aggregate With Labeled Parts As » qui reclasse certains agrégats étiquetés. |
| NeighborFinder | Filtering & Routing |  | duckdb |  | `planned_duckdb` | courant | Test spatial exclusivement fondé sur la distance/proximité (le plus proche voisin), pas sur intersects/contains — confirmé contre la documentation FME (« underlying spatial test is exclusively distance-based/nearest-neighbor » ; pas de count natif en attribut standard, seulement _distance/_angle) : ne correspond ni à transform.intersection (ON ST_Intersects) ni à transform.countWithin (predicate intersects\|contains seulement, core/app/pipelines/ops/schemas.py::TransformCountWithinParams). DuckDB spatial expose ST_Distance mais aucun op pipeline actuel n'implémente de jointure par plus-proche-voisin ; à concevoir. |
| PlanarityFilter | Filtering & Routing |  | duckdb |  | `planned_duckdb` | niche | Aucune fonction DuckDB spatial équivalente identifiée (contrairement à ST_IsClosed/ST_ConvexHull pour les deux filtres voisins) — un vrai test de planéité (tous les sommets coplanaires en 3D) exige un calcul géométrique dédié (ajustement de plan par moindres carrés ou produit vectoriel des normales), pas une simple fonction scalaire existante. Nécessiterait une nouvelle fonction ou un op dédié. |
| Sampler | Filtering & Routing |  | duckdb |  | `planned_duckdb` | courant | Aucun des 3 modes (chaque Nième, N premières, N dernières) n'est une expression scalaire bornée sans référence de table — app/pipelines/expr_validation.py::validate_bounded_expr rejette explicitement toute expression qui référence une table, ce qu'exigeraient ROW_NUMBER() OVER (...) ou LIMIT. DuckDB supporte nativement ces opérations (ROW_NUMBER()/QUALIFY, LIMIT) — il manque un nouvel op dédié (ex. transform.sample) exposant une vraie requête plutôt qu'une expression scalaire, pas une limite du moteur. Vérifié en confirmant que le Sampler FME n'a pas de mode pourcentage/aléatoire simple qui aurait été exprimable via random() — les 3 modes réels documentés exigent tous un ordre/une position dans l'ensemble. |
| SpatialFilter | Filtering & Routing | transform.intersection | duckdb |  | `implemented` | courant | how="inner", outputGeometry="left" reproduit le port Passed (ne garde que les Candidates qui intersectent au moins une feature Filter, géométrie Candidate inchangée). Écart non corrigé : transform.intersection est une jointure — une Candidate qui intersecte plusieurs features Filter en ressort dupliquée une fois par correspondance, alors que SpatialFilter ne produit qu'une seule feature Passed par Candidate. Le port Failed (comparaison inverse) n'a pas d'équivalent direct, même limite que Tester. |
| SpatialRelator | Filtering & Routing |  | duckdb |  | `planned_duckdb` | niche | Pas un routeur multi-port (une seule sortie principale + <Rejected>) — le vrai manque est l'enrichissement par jointure spatiale : transform.intersection ne joint que sur intersects et ne produit ni compte ni classification de relation ; transform.countWithin ne compte que sur intersects/contains (predicate: Literal["intersects", "contains"], core/app/pipelines/ops/schemas.py::TransformCountWithinParams). Élargir cet enum de prédicats OGC (Touches/Within/Crosses/Overlaps/Equals — fonctions DuckDB spatial ST_Touches/ST_Within/ST_Crosses/ST_Overlaps/ST_Equals plausibles mais non vérifiées individuellement ici) rapprocherait la couverture sans introduire de nouvel op. |
| TestFilter | Filtering & Routing |  | duckdb |  | `planned_duckdb` | courant | Chaque branche nommée est individuellement un WHERE trivial pour transform.filter, et le fan-out (N+1 nœuds transform.filter distincts branchés sur le même prédécesseur, un par branche nommée plus <Unfiltered>) est déjà supporté par la topologie du DAG (core/app/pipelines/compiler.py::predecessor_id ne borne que les arêtes ENTRANTES d'un nœud, jamais ses arêtes sortantes — le fan-out n'est pas la limite). La vraie limite : TestFilter évalue une liste ORDONNÉE de conditions et route vers la PREMIÈRE satisfaite (« first match wins ») — des nœuds transform.filter indépendants n'ont aucun moyen de garantir entre eux une priorité d'évaluation ni une mutuelle exclusivité, qu'aucun outil de ce dépôt ne vérifie aujourd'hui (cas Router explicite de la méthode de catégorisation). |
| Tester | Filtering & Routing | transform.filter | duckdb |  | `implemented` | courant | Le port Passed correspond directement à une clause WHERE bornée transform.filter (app/pipelines/expr_validation.py autorise toute expression scalaire SQL DuckDB sans référence de table) — transformer le plus utilisé de FME, exactement le cas d'usage visé par la méthode de catégorisation de cette tâche. Le port Failed (features rejetées) n'a pas d'équivalent direct dans un seul nœud transform.filter : il faudrait un second nœud avec la condition niée, branché en parallèle sur le même prédécesseur (fan-out supporté par la topologie linéaire+join, cf. core/app/pipelines/compiler.py::predecessor_id qui ne borne que les arêtes entrantes d'un nœud, pas les sortantes). |
| AffineWarper | Geometry |  | gdal | MIT/X (GDAL/OGR) | `planned_gdal` | niche | Recalage géométrique par points de contrôle (affine/polynomial) — proche du géoréférencement par GCP que GDAL sait faire côté raster (gdaltransform, gdalwarp -order) ; pas de crate Rust mature identifiée en recherche limitée à cette tâche, classé planned_gdal plutôt qu'un planned_rust deviné. |
| Aggregator | Geometry | transform.aggregate | duckdb |  | `implemented` | courant | `metrics` de transform.aggregate accepte une expression SQL arbitraire par colonne de sortie — `ST_Collect(geometry)` (fonction d'agrégation confirmée dans la documentation DuckDB spatial : « Collects a list of geometries into a collection geometry ») reproduit exactement le comportement d'Aggregator (rassemblement sans fusion de frontières, à la différence de native:dissolve/ST_Union_Agg déjà couvert par la ligne Dissolver de ce fichier). L'agrégation d'attributs seuls (sans géométrie) est le cas `groupBy`+`metrics` standard déjà utilisé par plusieurs lignes de ce fichier (ex. DuplicateFilter). |
| AreaAmalgamator | Geometry |  | gdal | MIT/X (GDAL/OGR) | `planned_gdal` | niche | Algorithme distinct de native:dissolve : amalgamation cartographique par enveloppe convexe/triangulation (généralisation — comble les petits espaces entre polygones voisins et produit une forme unique et cohérente), pas une simple fusion/union topologique — native:dissolve fusionne toutes les entités en une seule (multipartie), indépendamment de l'adjacence, quand aucun champ n'est renseigné (paramètre optionnel ; sauf activation du paramètre « conserver les entités disjointes »), ou par valeur d'attribut partagée sinon ; aucun des deux cas ne correspond à ce que fait AreaAmalgamator — pas de correspondance 1:1 honnête. |
| AreaBuilder | Geometry |  | gdal | MIT/X (GDAL/OGR) | `planned_gdal` | niche | Polygonisation d'un réseau de lignes vectorielles (proche de GEOS Polygonizer) — distincte de gdal:polygonize (qui vectorise un raster classé, pas un réseau de lignes) ; pas de correspondance qgis_frozen honnête. |
| AreaGapAndOverlapCleaner | Geometry |  | gdal | MIT/X (GDAL/OGR) | `planned_gdal` | niche | Nettoyage topologique de couverture polygonale — distinct de native:fixgeometries (répare la validité OGC d'une géométrie individuelle, ne traite pas les interstices/chevauchements entre polygones voisins). |
| AreaOnAreaOverlayer | Geometry | native:union | qgis | GPL-2.0-or-later (QGIS) | `qgis_frozen` | courant | native:union sans OVERLAY (paramètre optionnel dans core/app/pipelines/ops/qgis_algorithms.json) fait exactement ce qu'AreaOnAreaOverlayer fait sur une seule couche : « vérifie les chevauchements entre features sur une même couche [...] crée des features séparées pour les parties qui se chevauchent et celles qui ne se chevauchent pas » (docs.qgis.org, vérifié) — comportement identique à la description FME. native:union accepte aussi un second OVERLAY (deuxième couche), cas que ce même algorithme QGIS gelé couvre nativement en un seul paramètre optionnel, sans op GeoStudio dédié à cette variante. |
| BoundingBoxReplacer | Geometry | qgis:minimumboundinggeometry | qgis | GPL-2.0-or-later (QGIS) | `qgis_frozen` | niche | qgis:minimumboundinggeometry propose les types « Rectangle englobant » et « Rectangle orienté minimal » — seuls les modes 2D sont couverts, pas le mode cube 3D aligné aux axes. |
| Bufferer | Geometry | transform.buffer | duckdb |  | `implemented` | courant | Correspondance directe : `distance`/`unit` (meters\|native) de transform.buffer couvrent le cas d'usage central de Bufferer, y compris la réduction (distance négative — TransformBufferParams.distance n'a aucune contrainte de signe, ST_Buffer accepte nativement une distance négative pour éroder un polygone). Les styles d'about/d'angle (end cap/corner styles) de Bufferer n'ont pas d'équivalent : ST_Buffer est appelé sans paramètre de style dans compile_transform_sql (core/app/pipelines/compiler.py), toujours arrondi par défaut. |
| CenterPointReplacer | Geometry | native:centroids | qgis | GPL-2.0-or-later (QGIS) | `qgis_frozen` | courant | Correspondance sur le mode « Center of Gravity Point » uniquement : native:centroids calcule le barycentre de la géométrie (« peut se trouver à l'extérieur des limites de la feature », docs.qgis.org, vérifié), exactement le centre de masse de CenterPointReplacer — attributs conservés à l'identique dans les deux cas (paramètre ALL_PARTS pour les multi-parties). Les deux autres modes (centre de la boîte englobante, point garanti à l'intérieur — ce dernier plus proche de qgis:pointonsurface, hors allowlist gelée) n'ont pas d'équivalent ici. |
| Chopper | Geometry |  | gdal | MIT/X (GDAL/OGR) | `planned_gdal` | niche | Découpage technique de géométrie pour respecter une contrainte de taille (nombre de sommets/longueur), sans équivalent dans l'allowlist QGIS gelée ni dans les op DuckDB actuels. |
| Clipper | Geometry | native:clip | qgis | GPL-2.0-or-later (QGIS) | `qgis_frozen` | courant | native:clip (INPUT découpé par OVERLAY polygonal) ne conserve que ce qui tombe à l'intérieur de l'emprise — équivalent exact du port Inside de Clipper (docs.qgis.org, vérifié : « seules les parties de l'entrée qui tombent dans les polygones de la couche de recouvrement sont ajoutées à la couche résultante », découpage physique des géométries à la frontière, pas un simple filtre). Le port Outside n'a pas d'équivalent dans cet algorithme (native:difference, hors périmètre de cette tâche, couvrirait ce second cas — 0 ligne ne le référence encore). |
| ContourGenerator | Geometry | gdal:contour | qgis | GPL-2.0-or-later (QGIS) | `qgis_frozen` | courant | Correspondance directe sur le chemin raster→contours : gdal:contour (BAND, INTERVAL, FIELD_NAME=nom d'attribut d'élévation, CREATE_3D) couvre le mode « raster MNT en entrée » de ContourGenerator, paramètres vérifiés un par un dans core/app/pipelines/ops/qgis_algorithms.json. Le second mode de ContourGenerator (triangulation de Delaunay à partir de points/lignes de rupture, sans raster) n'a pas d'équivalent dans gdal:contour, dont INPUT est toujours typé raster (aucune variante vectorielle dans l'allowlist gelée). |
| Creator | Geometry | transform.createGeometry | duckdb |  | `implemented` | niche | Implémenté : transform.createGeometry remplace la géométrie par un WKT littéral pour chaque ligne en entrée — nécessite que l'entrée porte au moins une ligne ; ne nécessite pas de colonne geometry pré-existante (bug corrigé lors de la revue finale de vague 1). |
| Deaggregator | Geometry | transform.explodeGeometry | duckdb |  | `implemented` | niche | Opération inverse d'Aggregator/ST_Collect : multiplierait le nombre de lignes en sortie (une ligne par sous-géométrie), un pattern qu'aucun des 19 op actuels ne supporte (transform.select/transform.derive produisent toujours exactement une ligne de sortie par ligne d'entrée, cf. compile_transform_sql). DuckDB le permettrait via une fonction table (type ST_Dump + UNNEST), mais aucun op pipeline n'expose aujourd'hui de sémantique table-valued — nouvel op à concevoir, pas une simple expression bornée. |
| Densifier | Geometry | native:densifygeometriesgivenaninterval | qgis | GPL-2.0-or-later (QGIS) | `qgis_frozen` | niche | Correspondance directe avec native:densifygeometriesgivenaninterval (densification par intervalle) — le mode « diviser en N parties égales » de Densifier n'a pas d'équivalent exact dans cet algorithme QGIS, seul le mode par intervalle est couvert. |
| Dissolver | Geometry | native:dissolve | qgis | GPL-2.0-or-later (QGIS) | `qgis_frozen` | courant | Correspondance directe avec native:dissolve (allowlist gelée transform.qgis) — même comportement documenté (fusion de polygones adjacents/chevauchants en supprimant les arêtes internes). |
| DonutHoleExtractor | Geometry |  | gdal | MIT/X (GDAL/OGR) | `planned_gdal` | niche | Opération inverse de native:deleteholes (qui supprime les trous plutôt que de les extraire en features séparées) — pas de correspondance 1:1 honnête dans l'allowlist gelée. |
| Generalizer | Geometry | native:simplifygeometries | qgis | GPL-2.0-or-later (QGIS) | `qgis_frozen` | courant | Correspondance partielle : seul le mode de généralisation par suppression de sommets façon Douglas-Peucker (algorithme « Douglas » de Generalizer) est couvert par native:simplifygeometries (méthode Distance) — les modes lissage (McMaster/NURBfit/Fillet), mesure et ajustement par régression n'ont pas d'équivalent dans cet algorithme QGIS. |
| GeometryCoercer | Geometry |  | gdal | MIT/X (GDAL/OGR) | `planned_gdal` | courant | Conversion de type de géométrie générique (GDAL/OGR sait convertir entre types simples via ogr2ogr -nlt) — aucun op pipeline GeoStudio actuel n'expose cette conversion en tant que transformation dédiée. |
| HullReplacer | Geometry | native:convexhull | qgis | GPL-2.0-or-later (QGIS) | `qgis_frozen` | niche | Correspondance uniquement pour le mode enveloppe convexe (native:convexhull) — le mode enveloppe concave (paramètre alpha) de HullReplacer n'a pas d'équivalent dans les 50 algorithmes QGIS gelés. |
| Intersector | Geometry |  | gdal | MIT/X (GDAL/OGR) | `planned_gdal` | niche | Reconfirmé (déjà écarté d'un précédent lot comme correspondance de transform.intersection) : Intersector fait de la fragmentation topologique (« noding ») d'un unique jeu de features contre lui-même, pas une jointure spatiale entre deux jeux de données — transform.intersection joint deux collections (`withCollectionId`, ON ST_Intersects) pour de l'attribution croisée, jamais de fragmentation de géométrie. Aucun des 50 algorithmes QGIS gelés ne fait de noding générique multi-features (vérifié dans core/app/pipelines/ops/qgis_algorithms.json). Rattaché à `planned_gdal` par défaut (opération vectorielle générique) plutôt qu'à un futur op DuckDB : l'existence d'une fonction de noding type ST_Node dans l'extension spatiale DuckDB n'a pas pu être confirmée avec certitude contre une source primaire en session — pas de moteur affirmé sans preuve directe. |
| LineCombiner | Geometry |  | gdal | MIT/X (GDAL/OGR) | `planned_gdal` | niche | Fusion de lignes connectées (proche de ST_LineMerge/GEOS) — aucun op DuckDB/QGIS gelé actuel ne l'expose, classé opération vecteur générique future. |
| LineOnAreaOverlayer | Geometry |  | gdal | MIT/X (GDAL/OGR) | `planned_gdal` | niche | Aucun des 19 op ni des 50 algorithmes QGIS gelés ne fragmente une géométrie ligne à la frontière d'un polygone (ou l'inverse) en préservant les deux portions distinctes — transform.intersection joint deux collections par ST_Intersects mais ne découpe jamais la géométrie en plusieurs morceaux, et native:clip (Clipper, ce même lot) ne conserve que la portion intérieure, jamais les deux ports. Besoin distinct de celui fermé par Clipper (0 ligne référençant native:union en mode ligne/aire avant ce lot). |
| MinimumSpanningCircleReplacer | Geometry | qgis:minimumboundinggeometry | qgis | GPL-2.0-or-later (QGIS) | `qgis_frozen` | niche | qgis:minimumboundinggeometry propose un type « Cercle circonscrit minimal » équivalent — seule la sortie géométrie est couverte, pas le mode attributs (centre+rayon) de MinimumSpanningCircleReplacer. |
| Offsetter | Geometry | transform.translateGeometry | duckdb |  | `implemented` | niche | Implémenté : transform.translateGeometry applique une translation 2D. Refuse les géométries 3D : ST_Translate corrompt la coordonnée Z dans cette version de DuckDB spatial (vérifié empiriquement, contrairement à ce que suggère la documentation DuckDB seule). |
| PointOnAreaOverlayer | Geometry | transform.countWithin | duckdb |  | `implemented` | courant | Ferme transform.countWithin (0 ligne le référençait comme geostudio_equivalent avant ce lot) : le paramètre « Overlap Count Attribute » de PointOnAreaOverlayer (« nombre de features point qu'elles contenaient », docs.safe.com, vérifié) correspond exactement à `countColumn` avec `predicate="intersects"` (zone en entrée `input_view`, points via `withCollectionId`) — FME considère un point sur la frontière comme « in », cohérent avec ST_Intersects plutôt qu'un ST_Contains strict. Le second usage de PointOnAreaOverlayer (chaque point reçoit en retour les attributs de sa/ses zone(s) contenante(s), jointure dans l'autre sens) n'est pas couvert par cet op à sens unique — relèverait plutôt de transform.intersection. |
| Rotator | Geometry | transform.rotateGeometry | duckdb |  | `implemented` | niche | Implémenté : transform.rotateGeometry fait pivoter la géométrie autour de son propre centroïde. Refuse les géométries 3D, même cause que transform.translateGeometry (ST_Translate corrompt Z). |
| RubberSheeter | Geometry |  | gdal | MIT/X (GDAL/OGR) | `planned_gdal` | niche | Recalage par vecteurs de contrôle (conflation) — GDAL propose un recalage par points de contrôle pour le raster (gdalwarp -tps) ; aucun outil OGR équivalent pour le vecteur aujourd'hui, classé candidat générique futur plutôt que deviner un moteur exact. |
| Scaler | Geometry | transform.scaleGeometry | duckdb |  | `implemented` | niche | Implémenté : transform.scaleGeometry met à l'échelle la géométrie autour de l'origine (0,0), pas du centre de la géométrie. |
| Snapper | Geometry | native:snapgeometries | qgis | GPL-2.0-or-later (QGIS) | `qgis_frozen` | niche | Correspondance directe avec native:snapgeometries (tolérance de distance, plusieurs modes de comportement de snap) — le filtrage par attribut commun de Snapper n'a pas d'équivalent direct dans l'algorithme QGIS, à faire en amont via transform.filter/transform.join si besoin. |
| SpikeRemover | Geometry |  | gdal | MIT/X (GDAL/OGR) | `planned_gdal` | niche | Nettoyage topologique par seuil d'angle — proche mais distinct de native:fixgeometries (répare les géométries invalides selon les règles OGC, ne détecte pas les pics par angle) ; pas de correspondance 1:1 honnête dans l'allowlist gelée. |
| TINGenerator | Geometry | native:delaunaytriangulation | qgis | GPL-2.0-or-later (QGIS) | `qgis_frozen` | niche | Même algorithme sous-jacent (triangulation de Delaunay sur un nuage de points) — les lignes de rupture (breaklines) de TINGenerator, qui contraignent la triangulation, n'ont pas d'équivalent dans native:delaunaytriangulation (triangulation de Delaunay pure sans contrainte). |
| TextureCoordinateSetter | Geometry |  | n/a |  | `unknown` | niche | Fonctionnalité de rendu 3D (texturage de maillage, ex. workflows CityEngine/3D Tiles) plutôt qu'une opération d'analyse spatiale — aucun moteur candidat (GDAL/PDAL/OTB/Rust) identifié en recherche limitée à cette tâche pour cette fonction précise ; à retrancher lors d'un futur passage dédié au 3D plutôt que deviner. |
| Triangulator | Geometry |  | gdal | MIT/X (GDAL/OGR) | `planned_gdal` | niche | Triangule l'intérieur d'un polygone/solide existant (maillage de rendu), un algorithme différent de la triangulation de Delaunay sur nuage de points (native:delaunaytriangulation) — pas de correspondance qgis_frozen honnête, classé opération vecteur générique future. |
| VertexRemover | Geometry |  | gdal | MIT/X (GDAL/OGR) | `planned_gdal` | niche | Suppression de sommets par index de position — différent de la simplification géométrique (native:simplifygeometries, qui retire des sommets selon un critère de tolérance spatiale), pas de correspondance QGIS honnête. |
| ArcGISBranchVersionManager | Integrations |  | n/a |  | `out_of_scope` | niche | Le versionnement par branches est un concept de géodatabase d'entreprise Esri propriétaire sans équivalent dans le modèle de données de GeoStudio (aucune notion de version/branche sur une collection) — capacité produit absente plutôt qu'un manque de moteur d'exécution. |
| ArcGISOnlineConnector | Integrations |  | duckdb | MIT (DuckDB) + API REST Esri (documentée publiquement, authentification par jeton) | `planned_duckdb` | courant | GeoStudio dispose déjà d'un connecteur de moissonnage ArcGIS FS (app/harvest/connectors, SP-12) mais celui-ci référence des métadonnées de catalogue pour la fédération STAC/DCAT, il ne fait pas transiter des données dans un pipeline no-code — les deux besoins sont distincts, aucun ne couvre l'autre. La sérialisation esriJSON (géométries non-GeoJSON) nécessiterait une traduction dédiée, d'où planned_duckdb plutôt qu'un usage nu de reader.connector.rest. |
| AutodeskBIM360DocsConnector | Integrations |  | duckdb | MIT (DuckDB) + API REST Autodesk Platform Services (accès développeur gratuit, OAuth2 — pas de SDK Python officiel dédié requis) | `planned_duckdb` | niche | API REST/OAuth2 client_credentials documentée publiquement (Autodesk Platform Services), proche du patron reader.connector.rest existant, mais la hiérarchie hub/projet/dossier/version nécessite plusieurs appels chaînés — connecteur dédié plutôt qu'un usage direct du connecteur REST générique. |
| AutodeskDocsConnector | Integrations |  | duckdb | MIT (DuckDB) + API REST Autodesk Platform Services (accès développeur gratuit, OAuth2) | `planned_duckdb` | niche | Même plateforme et même API qu'AutodeskBIM360DocsConnector — même jugement. |
| AzureBlobStorageConnector | Integrations |  | duckdb | MIT (DuckDB) + Apache-2.0 (dlt, source filesystem via fsspec/adlfs, MIT) | `planned_duckdb` | courant | Même patron que S3Connector — le protocole az:// est déjà supporté par la source filesystem générique de dlt. |
| AzureFileStorageConnector | Integrations |  | duckdb | MIT (DuckDB) + MIT (azure-storage-file-share, SDK officiel Azure) | `planned_duckdb` | niche | À distinguer d'AzureBlobStorageConnector : Azure Files est un partage SMB, non couvert par la source filesystem générique de dlt (limitée à s3/az blob/gcs) — nécessiterait un connecteur dédié au SDK azure-storage-file-share plutôt qu'une extension de fsspec. |
| AzureQueueStorageConnector | Integrations |  | duckdb | MIT (DuckDB) + MIT (azure-storage-queue, SDK officiel Azure) | `planned_duckdb` | niche | Même réserve que RabbitMQConnector/AzureServiceBusConnector (Q10, REV-108). |
| AzureServiceBusConnector | Integrations |  | duckdb | MIT (DuckDB) + MIT (azure-servicebus, SDK officiel Azure) | `planned_duckdb` | niche | Même réserve que RabbitMQConnector : sondage périodique envisageable, consommation continue temps réel hors périmètre actuel (Q10, REV-108). |
| BoxConnector | Integrations |  | duckdb | MIT (DuckDB) + Apache-2.0 (boxsdk, SDK officiel Python) | `planned_duckdb` | niche | Même famille que DropboxConnector — SDK officiel permissif, connecteur dédié à écrire. |
| CKANConnector | Integrations |  | duckdb | MIT (DuckDB) + Apache-2.0 (dlt) + client CKAN sous licence MIT (ckanapi) — le serveur CKAN lui-même est publié sous AGPL-3.0, licence sans effet sur un simple appel client à son API publique | `planned_duckdb` | niche | Distinct du connecteur de moissonnage CKAN déjà livré (app/harvest/connectors, SP-12g) : celui-ci référence des métadonnées de catalogue pour la fédération STAC/DCAT, alors que ce transformer FME fait transiter les données elles-mêmes dans un pipeline — les deux répondent à des besoins différents, aucun ne couvre l'autre. L'API Action CKAN (package_search/resource_show, pagination rows/start) est un JSON REST assez générique pour envisager reader.connector.rest directement, mais la résolution multi-étapes (recherche→jeu de données→ressource→fichier) motive un connecteur dédié plutôt qu'un usage nu du connecteur générique. |
| CityEngineModelGenerator | Integrations |  | n/a | Moteur de règles procédurales CGA propriétaire Esri (nécessite CityEngine ou ArcGIS Urban) — aucune implémentation ouverte équivalente connue | `license_blocked` | niche | Génération procédurale 3D propriétaire Esri, sans équivalent ouvert. |
| DropboxConnector | Integrations |  | duckdb | MIT (DuckDB) + MIT (dropbox, SDK officiel Python) | `planned_duckdb` | niche | SDK officiel permissif, mais hors du patron fsspec/dlt filesystem déjà vérifié pour S3/Azure Blob/GCS — connecteur dédié à écrire. |
| Emailer | Integrations |  | duckdb | MIT (DuckDB) + bibliothèque standard Python smtplib (licence PSF) | `planned_duckdb` | courant | GeoStudio a déjà un canal e-mail pour les alertes (AlertRule, SP-16b/SP-53) mais aucun writer générique d'envoi d'e-mail utilisable par n'importe quel pipeline no-code — les deux capacités ne se recouvrent pas au niveau des 19 ops actuels. |
| FMEFlowJobSubmitter | Integrations |  | n/a |  | `out_of_scope` | niche | Orchestration interne à l'écosystème du produit concurrent FME Flow — aucun cas d'usage réaliste pour une plateforme GeoStudio qui ne fait pas tourner FME Flow à ses côtés. |
| FMEFlowNotifier | Integrations |  | n/a |  | `out_of_scope` | niche | Même famille que FMEFlowJobSubmitter — orchestration interne au produit concurrent FME Flow. |
| FTPCaller | Integrations |  | duckdb | MIT (DuckDB) + bibliothèque standard Python ftplib (licence PSF) pour FTP/FTPS, ou dlt (source filesystem via fsspec/paramiko, LGPL-2.1) pour SFTP | `planned_duckdb` | courant | Échange de fichiers encore courant avec les infrastructures publiques françaises (portails de données, livraisons IGN) — le protocole SFTP est couvert par la même source filesystem générique dlt que S3/Azure/GCS (fsspec) plutôt qu'un connecteur ad hoc. |
| Geocoder | Integrations |  | duckdb | MIT (DuckDB) + MIT (geopy) pour un fournisseur ouvert par défaut (OSM Nominatim, licence de données ODbL, usage soumis à la politique d'utilisation Nominatim) | `planned_duckdb` | courant | Même famille que HTTPCaller/SQLExecutor : un appel externe par ligne traitée plutôt qu'une lecture unique en tête de DAG — capacité d'appel réseau par ligne à ajouter à l'architecture des transforms, pas seulement un nouveau connecteur de lecture. |
| GoogleCloudStorageConnector | Integrations |  | duckdb | MIT (DuckDB) + Apache-2.0 (dlt, source filesystem via fsspec/gcsfs, BSD-3-Clause) | `planned_duckdb` | niche | Même patron que S3Connector/AzureBlobStorageConnector — protocole gs:// déjà supporté par la source filesystem générique de dlt. |
| GoogleDriveConnector | Integrations |  | duckdb | MIT (DuckDB) + Apache-2.0 (google-api-python-client, SDK officiel Google) | `planned_duckdb` | niche | SDK officiel permissif, connecteur dédié à écrire (hors patron fsspec/dlt filesystem déjà vérifié). |
| HDFSConnector | Integrations |  | duckdb | MIT (DuckDB) + Apache-2.0 (pyarrow, bindings HDFS libhdfs) | `planned_duckdb` | niche | Licence des bibliothèques Python permissive, mais l'accès réel exige une bibliothèque native libhdfs et un cluster Hadoop/JVM côté serveur — dépendance opérationnelle bien plus lourde qu'un simple paquet Python, peu prioritaire pour une plateforme SIG cloud-native qui ne cible pas les écosystèmes Hadoop on-premise. |
| IBMMQConnector | Integrations |  | n/a | pymqi (le client Python, Python Software Foundation License) dépend des bibliothèques clientes IBM MQ, redistribuables gratuitement mais sous licence propriétaire IBM (International Program License Agreement), non open-source | `license_blocked` | niche | Système de messagerie d'entreprise legacy — le blocage porte sur le client natif IBM requis par pymqi, pas sur le protocole AMQP/JMS lui-même. |
| JMSReceiver | Integrations |  | n/a |  | `unknown` | niche | JMS est une API Java, sans équivalent Python natif direct — un pont serait requis (bibliothèque Java via passerelle, ou protocole STOMP si le courtier l'expose) : recherche insuffisante en session pour trancher entre ces chemins selon le courtier réellement visé. |
| JMSSender | Integrations |  | n/a |  | `unknown` | niche | Même réserve que JMSReceiver — API Java sans équivalent Python natif direct, recherche insuffisante pour trancher. |
| KinesisReceiver | Integrations |  | duckdb | MIT (DuckDB) + Apache-2.0 (boto3, SDK officiel AWS) | `planned_duckdb` | niche | Même réserve que les autres files de messages : sondage périodique (GetRecords) envisageable, streaming continu hors périmètre actuel (Q10, REV-108). |
| KinesisSender | Integrations |  | duckdb | MIT (DuckDB) + Apache-2.0 (boto3, SDK officiel AWS) | `planned_duckdb` | niche | Symétrique de KinesisReceiver côté écriture — même lacune de writer externe générique que DatabaseUpdater. |
| MCPCaller | Integrations |  | duckdb | MIT (DuckDB) + MIT (mcp, SDK officiel Python du Model Context Protocol, vérifié sur PyPI) | `planned_duckdb` | niche | GeoStudio héberge déjà son propre serveur MCP (même process que le cœur, SP-2/SP-20) mais n'a aucune capacité symétrique d'appeler un serveur MCP tiers depuis un pipeline no-code — un futur reader/transform.connector.mcp suivrait un patron proche de reader.connector.rest (JSON-RPC 2.0 sur HTTP/SSE au lieu de REST simple). |
| OneDriveConnector | Integrations |  | duckdb | MIT (DuckDB) + MIT (msgraph-sdk, SDK officiel Microsoft) | `planned_duckdb` | niche | SDK officiel permissif (Microsoft Graph), connecteur dédié à écrire. |
| ProjectWiseWSGConnector | Integrations |  | n/a | Écosystème Bentley ProjectWise/iTwin propriétaire — accès nécessite une licence Bentley et le SDK associé, aucun client ouvert documenté | `license_blocked` | niche | Gestion documentaire d'ingénierie propriétaire Bentley, sans équivalent ouvert connu. |
| RCaller | Integrations |  | n/a | R lui-même est publié sous licence GPL-2/GPL-3 (R Foundation) — même famille de contrainte que QGIS (transform.qgis), mais aucun mécanisme de sidecar isolé équivalent n'existe aujourd'hui pour R | `license_blocked` | niche | Calcul statistique externe sous licence copyleft forte — traitement cohérent avec la posture déjà retenue pour QGIS (sidecar GPL isolé, jamais une catégorie d'op normale), mais aucun sidecar R n'existe à ce jour dans le dépôt. |
| RabbitMQConnector | Integrations |  | duckdb | MIT (DuckDB) + BSD-3-Clause (pika, client AMQP officiel) | `planned_duckdb` | niche | Ingestion par sondage périodique (consommation d'un lot de messages en attente à chaque exécution planifiée) envisageable sans nouveau moteur — la consommation continue temps réel reste hors de l'architecture cron/webhook actuelle des pipelines GeoStudio (question produit Q10 temps réel, REV-108, toujours ouverte). |
| S3Connector | Integrations |  | duckdb | MIT (DuckDB) + Apache-2.0 (dlt, source filesystem via fsspec/s3fs, BSD-3-Clause) | `planned_duckdb` | courant | dlt fournit nativement une source filesystem générique sur les protocoles s3/az/gcs via fsspec (vérifié dans la doc dlt, dlthub.com/docs/dlt-ecosystem/verified-sources/filesystem) — un reader.connector.blob suivant le même patron que reader.connector.postgres couvrirait S3 sans nouveau moteur d'exécution. |
| SNSSender | Integrations |  | duckdb | MIT (DuckDB) + Apache-2.0 (boto3, SDK officiel AWS) | `planned_duckdb` | niche | Fonctionnellement proche du canal e-mail/webhook déjà livré sur AlertRule (SP-16b/SP-53), mais à l'échelle d'une étape de pipeline générique plutôt que d'une règle d'alerte dédiée — point d'insertion différent dans l'architecture, aucun recouvrement direct. |
| SharePointOnlineConnector | Integrations |  | duckdb | MIT (DuckDB) + MIT (msgraph-sdk, SDK officiel Microsoft) | `planned_duckdb` | niche | Même SDK que OneDriveConnector (Microsoft Graph) — pertinence plausible pour des collectivités déjà sous Microsoft 365, non vérifiée par un cas d'usage réel en session. |
| SlackConnector | Integrations |  | duckdb | MIT (DuckDB) + MIT (slack_sdk, SDK officiel Slack) | `planned_duckdb` | courant | Notification d'exécution de pipeline — même famille de besoin que le canal webhook déjà livré sur AlertRule, mais générique à toute étape d'un pipeline plutôt que réservé à une transition d'état d'alerte. |
| TCPIPReceiver | Integrations |  | n/a |  | `unknown` | niche | Écoute réseau continue, incompatible avec le modèle d'exécution batch/cron/webhook actuel des pipelines GeoStudio (jamais un processus longuement à l'écoute) — même question produit ouverte que le temps réel (Q10, REV-108). |
| TCPIPSender | Integrations |  | n/a |  | `unknown` | niche | Symétrique de TCPIPReceiver — même limitation architecturale (Q10, REV-108). |
| TransporterReceiver | Integrations |  | n/a |  | `out_of_scope` | niche | Mécanisme de transport propriétaire interne à l'écosystème Safe Software (FME↔FME), sans protocole ouvert documenté à réimplémenter — aucune pertinence hors de l'écosystème FME lui-même. |
| TransporterSender | Integrations |  | n/a |  | `out_of_scope` | niche | Symétrique de TransporterReceiver — même mécanisme propriétaire interne à l'écosystème Safe Software. |
| TrelloConnector | Integrations |  | duckdb | MIT (DuckDB) + BSD License (py-trello, client non officiel) | `planned_duckdb` | niche | Client non officiel mais permissif — connecteur de notification/suivi de tâches, faible priorité pour une plateforme géospatiale. |
| TrimbleConnectConnector | Integrations |  | duckdb | MIT (DuckDB) + API REST Trimble Connect (accès développeur via clé API, pas de SDK Python officiel identifié) | `planned_duckdb` | niche | API REST publique avec authentification par clé/OAuth — proche du patron reader.connector.rest générique, mais non vérifié en session faute de compte développeur Trimble. |
| TrimbleUnityConnector | Integrations |  | n/a |  | `unknown` | niche | Peu de documentation publique trouvée en session sur l'ouverture et la forme de l'API Trimble Unity — recherche insuffisante pour trancher entre connecteur REST générique et blocage de licence. |
| DEMGenerator | Point Cloud | qgis:tininterpolation | qgis | GPL-2.0-or-later (QGIS) | `qgis_frozen` | courant | qgis:tininterpolation construit également une triangulation à partir de points avec champ Z puis échantillonne un raster de sortie (TIN → grille régulière) — même principe. Les lignes de rupture (breaklines) de DEMGenerator, qui contraignent la triangulation, n'ont pas d'équivalent dans la version QGIS gelée (même réserve que TINGenerator/SurfaceModeller). |
| PointCloudCombiner | Point Cloud |  | pdal | BSD-3-Clause (PDAL) | `planned_pdal` | courant | Fusion/concaténation de nuages de points (type PDAL merge filter) — aucun des 50 algorithmes QGIS gelés ne traite de nuages de points natifs. |
| PointCloudCreator | Point Cloud |  | pdal | BSD-3-Clause (PDAL) | `planned_pdal` | niche | Génération de nuage de points synthétique — hors des 50 algorithmes QGIS gelés, usage principalement pour les tests de workflow. |
| PointCloudExpressionEvaluator | Point Cloud |  | pdal | BSD-3-Clause (PDAL) | `planned_pdal` | niche | Équivalent conceptuel de transform.derive mais au niveau POINT d'un nuage natif, pas au niveau ligne tabulaire — hors des 50 algorithmes QGIS gelés et hors périmètre de transform.derive (DuckDB) actuel. |
| PointCloudFilter | Point Cloud |  | pdal | BSD-3-Clause (PDAL) | `planned_pdal` | courant | Filtrage/routage par expression sur un nuage de points (type PDAL range/expression filter, ex. filtrage par classification LiDAR) — hors des 50 algorithmes QGIS gelés. |
| PointCloudMerger | Point Cloud |  | pdal | BSD-3-Clause (PDAL) | `planned_pdal` | niche | Jointure par composant entre nuages de points (proche d'un merge/join PDAL par attribut plutôt qu'une simple concaténation) — sans équivalent parmi les 50 algorithmes QGIS gelés. |
| PointCloudSimplifier | Point Cloud |  | pdal | BSD-3-Clause (PDAL) | `planned_pdal` | niche | Simplification préservant la forme (proche d'un filtre de sous-échantillonnage adaptatif, type PDAL) — distincte du simple décimage de PointCloudThinner ; hors des 50 algorithmes QGIS gelés. |
| PointCloudSorter | Point Cloud |  | pdal | BSD-3-Clause (PDAL) | `planned_pdal` | niche | Tri par attribut sur un nuage de points — hors des 50 algorithmes QGIS gelés. |
| PointCloudSplitter | Point Cloud |  | pdal | BSD-3-Clause (PDAL) | `planned_pdal` | courant | Éclatement par classification/attribut (ex. isoler le sol des autres classes LiDAR) — hors des 50 algorithmes QGIS gelés. |
| PointCloudStatisticsCalculator | Point Cloud |  | pdal | BSD-3-Clause (PDAL) | `planned_pdal` | niche | Statistiques par composant sur un nuage de points natif — distinct de transform.aggregate (GeoStudio, tabulaire/DuckDB) qui n'a aucune notion de composant de nuage de points ; hors des 50 algorithmes QGIS gelés. |
| PointCloudSurfaceBuilder | Point Cloud |  | pdal | BSD-3-Clause (PDAL) | `planned_pdal` | niche | Opère exclusivement sur la structure de données nuage de points dédiée de FME (pas des points vectoriels génériques) — donc pas le même périmètre que qgis:tininterpolation/native:delaunaytriangulation (qui prennent des points vectoriels avec champ Z) : pas de match qgis_frozen forcé malgré la parenté conceptuelle avec DEMGenerator/SurfaceModeller ci-dessous. |
| PointCloudThinner | Point Cloud |  | pdal | BSD-3-Clause (PDAL) | `planned_pdal` | courant | Décimation de nuage de points (type PDAL decimation filter) — hors des 50 algorithmes QGIS gelés. |
| PointCloudToPointCoercer | Point Cloud |  | pdal | BSD-3-Clause (PDAL) | `planned_pdal` | courant | Passerelle nuage de points → géométrie vectorielle standard (type PDAL writers.text/ogr après lecture LAS) — nécessaire avant qu'un op vectoriel GeoStudio existant (transform.*) puisse traiter les points ; hors des 50 algorithmes QGIS gelés. |
| PointCloudTransformationApplier | Point Cloud |  | pdal | BSD-3-Clause (PDAL) | `planned_pdal` | niche | Application de transformation affine sur un nuage de points (type PDAL filters.transformation) — hors des 50 algorithmes QGIS gelés. |
| SurfaceDraper | Point Cloud |  | gdal | MIT/X (GDAL/OGR) | `planned_gdal` | niche | Opération composite (construction de surface + échantillonnage d'élévation sur des features tierces + calcul de pente/exposition dérivé) — pas un match qgis_frozen à un algorithme unique (la méthode de catégorisation exclut les compositions). L'implémentation ouverte la plus courante pour draper des features vectorielles sur un MNT est un échantillonnage ponctuel de raster (gdallocationinfo/rasterio) plutôt qu'un nuage de points PDAL, d'où planned_gdal malgré le classement FME sous « Point Clouds ». |
| SurfaceModeller | Point Cloud | native:delaunaytriangulation | qgis | GPL-2.0-or-later (QGIS) | `qgis_frozen` | niche | Même algorithme sous-jacent que TINGenerator déjà couvert dans ce fichier (triangulation de Delaunay) — FME expose les deux comme des transformers distincts. Même nuance déjà documentée pour TINGenerator : les breaklines qui contraignent la triangulation n'ont pas d'équivalent dans native:delaunaytriangulation (Delaunay pure, sans contrainte). |
| GoogleVisionConnector | Raster |  | n/a |  | `out_of_scope` | niche | Intégration à un service SaaS propriétaire spécifique (Google Cloud Vision, facturation à l'usage) — pas une capacité géospatiale générique à implémenter en interne, même famille de décision que FMEFlowNotifier/FMEFlowJobSubmitter (intégration à un produit tiers précis). Des alternatives ouvertes existent pour la détection d'objets sur imagerie (OpenCV, OTB) mais ne remplacent pas cette intégration spécifique — non license_blocked : le blocage n'est pas une licence GPL, c'est un compte commercial tiers. |
| ImageFetcher | Raster |  | gdal | MIT/X (GDAL/OGR) | `planned_gdal` | niche | GDAL sait lire directement une image distante via /vsicurl/ — proche en esprit de reader.connector.rest mais la sortie est une géométrie raster, pas une ligne tabulaire : rattaché au bucket raster générique plutôt qu'à un connecteur DuckDB. |
| RasterAspectCalculator | Raster | native:aspect | qgis | GPL-2.0-or-later (QGIS) | `qgis_frozen` | courant | Correspondance directe avec native:aspect (INPUT/OUTPUT/Z_FACTOR), même convention d'angle. |
| RasterBandCombiner | Raster |  | gdal | MIT/X (GDAL/OGR) | `planned_gdal` | niche | Empilement de bandes (gdal_merge.py -separate / gdalbuildvrt -separate) — absent des 50 algorithmes QGIS gelés. |
| RasterBandNodataSetter | Raster |  | gdal | MIT/X (GDAL/OGR) | `planned_gdal` | niche | gdal_edit.py -a_nodata / gdal_translate -a_nodata — absent des 50 algorithmes QGIS gelés. |
| RasterBandRemover | Raster |  | gdal | MIT/X (GDAL/OGR) | `planned_gdal` | niche | gdal_translate -b avec liste de bandes à conserver — absent des 50 algorithmes QGIS gelés. |
| RasterBandSeparator | Raster |  | gdal | MIT/X (GDAL/OGR) | `planned_gdal` | niche | Opération inverse de RasterBandCombiner (gdal_translate -b) — même bucket moteur candidat. |
| RasterCellValueCalculator | Raster |  | gdal | MIT/X (GDAL/OGR) | `planned_gdal` | courant | Algèbre raster générique (type gdal_calc.py) — aucun des 50 algorithmes QGIS gelés ne l'expose (le plus proche, native:fieldcalculator, est vectoriel/attributaire, pas raster). |
| RasterConvolver | Raster |  | gdal | MIT/X (GDAL/OGR) | `planned_gdal` | niche | Filtrage par noyau de convolution générique — hors des 50 algorithmes QGIS gelés (aucun filtre de convolution raster générique dans la liste). |
| RasterExtractor | Raster |  | gdal | MIT/X (GDAL/OGR) | `planned_gdal` | niche | Équivalent conceptuel d'un writer.export raster vers un attribut binaire (gdal_translate vers une mémoire tampon MEM/PNG/TIFF) — aucun op pipeline GeoStudio actuel ne traite de raster brut. |
| RasterHillshader | Raster | native:hillshade | qgis | GPL-2.0-or-later (QGIS) | `qgis_frozen` | courant | Correspondance directe sur l'opération (azimut, angle vertical) — RasterHillshader n'a en réalité aucun paramètre facteur Z côté FME (contrairement à une affirmation initiale erronée) : la doc FME indique qu'un Scaler en amont est nécessaire en cas d'unités horizontales/verticales différentes, là où native:hillshade expose ce facteur directement. |
| RasterMosaicker | Raster |  | gdal | MIT/X (GDAL/OGR) | `planned_gdal` | courant | Territoire classique gdal_merge.py/gdalbuildvrt — aucun équivalent dans les 50 algorithmes QGIS gelés (mosaïquage absent de la liste). |
| RasterObjectDetectionModelTrainer | Raster |  | otb | Apache-2.0 (Orfeo ToolBox) | `planned_otb` | niche | Même famille dépréciée (FME 2025.0) que RasterObjectDetector — même jugement de bucket et même réserve, cf. rapport. |
| RasterObjectDetector | Raster |  | otb | Apache-2.0 (Orfeo ToolBox) | `planned_otb` | niche | Transformer déprécié par Safe Software depuis FME 2025.0 (vérifié sur la page de documentation officielle). Technique historique (cascades Haar/LBP OpenCV, licence BSD), pas strictement de la télédétection OTB classique — rapproché malgré tout de planned_otb comme bucket « classification/détection sur imagerie avancée », faute de bucket dédié à la vision par ordinateur classique dans la méthode de catégorisation ; jugement à valider (cf. rapport). |
| RasterPyramider | Raster |  | gdal | MIT/X (GDAL/OGR) | `planned_gdal` | niche | Construction de pyramides/overviews (gdaladdo) — absent des 50 algorithmes QGIS gelés. |
| RasterResampler | Raster | gdal:warpreproject | qgis | GPL-2.0-or-later (QGIS) | `qgis_frozen` | courant | gdal:warpreproject (« Warp (reproject) ») accepte TARGET_RESOLUTION + RESAMPLING sans changer TARGET_CRS — un pur rééchantillonnage est donc possible sans reprojection, même si l'algorithme QGIS couvre en plus un cas (reprojection) que RasterResampler ne traite pas. |
| RasterSegmenter | Raster |  | otb | Apache-2.0 (Orfeo ToolBox) | `planned_otb` | niche | Segmentation d'image (type Mean-Shift/watershed) — relève de la télédétection avancée, module Segmentation d'Orfeo ToolBox, pas des 50 algorithmes QGIS gelés (aucun algorithme de segmentation raster dans la liste). |
| RasterSlopeCalculator | Raster | native:slope | qgis | GPL-2.0-or-later (QGIS) | `qgis_frozen` | courant | native:slope (INPUT/OUTPUT/Z_FACTOR) calcule la même pente en degrés — RasterSlopeCalculator offre en plus un choix d'algorithme et une sortie en pourcentage que la version QGIS gelée n'expose pas explicitement (couverture partielle, mais le comportement par défaut correspond). |
| RasterStatisticsCalculator | Raster |  | gdal | MIT/X (GDAL/OGR) | `planned_gdal` | courant | Statistiques globales par bande (gdalinfo -stats) — à distinguer de native:rasterlayerzonalstats/native:zonalstatisticsfb (déjà dans l'allowlist QGIS), qui calculent des statistiques PAR ZONE vectorielle et non sur le raster entier : pas le même périmètre, donc pas de match qgis_frozen forcé. |
| RasterToPolygonCoercer | Raster | gdal:polygonize | qgis | GPL-2.0-or-later (QGIS) | `qgis_frozen` | courant | Correspondance directe avec gdal:polygonize (« Polygonize (raster to vector) »), même principe de traçage de contour par zone de valeur homogène. |
| VectorOnRasterOverlayer | Raster |  | gdal | MIT/X (GDAL/OGR) | `planned_gdal` | niche | Pas un match qgis_frozen : gdal:rasterize (QGIS) ne prend pas de raster existant en entrée (il crée toujours un nouveau raster de sortie depuis un WIDTH/HEIGHT/EXTENT) et ne gère pas les nuages de points — deux différences de comportement réelles, pas une simple divergence de nommage. gdal_rasterize en ligne de commande sait bien burn onto an existing raster (-a sur un fichier pré-existant), d'où planned_gdal plutôt qu'un abandon en unknown. |
| WebMapTiler | Raster |  | gdal | MIT/X (GDAL/OGR) | `planned_gdal` | courant | Territoire classique gdal2tiles.py — absent des 50 algorithmes QGIS gelés. Distinct du service de tuiles vectorielles ST_AsMVT déjà servi par le cœur (SP-24) : ici il s'agit de produire des tuiles RASTER statiques depuis un fichier, pas de servir des tuiles vectorielles dynamiques depuis une collection. |
| ASPRS LiDAR Data Exchange Format (LAS/LAZ) | Readers/Writers |  | pdal | BSD-3-Clause (PDAL) | `planned_pdal` | niche |  |
| ASTM E57 | Readers/Writers |  | pdal | BSL-1.0 (PDAL, pilote E57 embarque un fork de libE57Format, sous licence Boost Software License 1.0) | `planned_pdal` | niche |  |
| Amazon Redshift | Readers/Writers | reader.connector.postgres | duckdb | MIT (DuckDB) + Apache-2.0 (dlt) | `implemented` | niche | D'après le chantier GAP-16 : compatibilité documentée par la littérature Amazon (protocole filaire Postgres) et non vérifiée sur un cluster réel en session — pas de nouveau code requis, reader.connector.postgres suffit tel quel. |
| Apache Parquet | Readers/Writers |  | duckdb | MIT (DuckDB, lecteur Parquet natif) | `planned_duckdb` | courant | Ingestion GeoStudio importe déjà le GeoParquet géospatial (SP-56, parse_geoparquet) et le Parquet tabulaire pur (GAP-29, parse_parquet_tabular) ; toujours aucun op de pipeline reader.* de fichier Parquet arbitraire. |
| Autodesk AutoCAD DWG | Readers/Writers |  | gdal | Nécessite le SDK propriétaire ODA/Teigha (licence commerciale) pour un support DWG complet via GDAL ; alternative LibreDWG sous GPL-3.0 (copyleft fort) | `license_blocked` | niche | Aucune implémentation DWG mature et permissive trouvée : soit SDK Autodesk/ODA commercial, soit LibreDWG GPL-3.0 (incompatible avec une intégration Apache-2.0 sans obligations de copyleft) — jugement distinct du DXF (texte, librement lisible par GDAL sans SDK tiers). |
| Autodesk AutoCAD DXF | Readers/Writers |  | gdal | MIT/X (GDAL/OGR, pilote DXF) | `planned_gdal` | niche | DXF est un format texte documenté publiquement par Autodesk, pilote GDAL natif sans dépendance propriétaire — distinct du binaire DWG (cf. ligne séparée). |
| Autodesk Revit | Readers/Writers |  | n/a | API Autodesk Revit / plateforme Forge (propriétaire, licence commerciale) — aucune bibliothèque open source ne lit le format .rvt natif (IfcOpenShell couvre l'export IFC, pas le format natif) | `license_blocked` | niche |  |
| Bentley MicroStation Design (DGN) | Readers/Writers |  | gdal | MIT/X (GDAL/OGR, pilote DGN v7 uniquement) | `planned_gdal` | niche | Le pilote DGN de GDAL ne couvre que le format DGN v7 (V8 non supporté en open source) — limitation à documenter si ce format est priorisé plus tard. |
| CSV (Comma-Separated Value) | Readers/Writers |  | duckdb | MIT (DuckDB, lecteur CSV natif) | `planned_duckdb` | courant | Ingestion GeoStudio importe déjà le CSV géoréférencé (lat/lon, WKT) ; aucun op pipeline reader.* de fichier CSV arbitraire (hors connecteurs SQL) n'existe. |
| CityGML | Readers/Writers |  | gdal | MIT/X (GDAL, lisible partiellement via le pilote générique GML ou le pilote GMLAS — aucun pilote CityGML dédié) ; une intégration dédiée utiliserait plus probablement libcitygml directement (LGPL-2.1, bibliothèque distincte, pas une dépendance de GDAL) | `planned_gdal` | niche | GDAL n'a pas de pilote CityGML dédié : la lecture partielle passe par le pilote générique GML (schéma non interprété sémantiquement) ou par le pilote GMLAS depuis GDAL 2.2 (mapping piloté par schéma XSD, tous deux MIT/X). Une intégration dédiée et complète du modèle CityGML (LOD1-4, sémantique des objets) utiliserait plus probablement libcitygml directement (LGPL-2.1) — une bibliothèque séparée qui peut optionnellement s'appuyer sur GDAL pour les transformations de coordonnées (LIBCITYGML_USE_GDAL), pas l'inverse. Écriture non couverte par cette voie. |
| CityJSON | Readers/Writers |  | gdal | MIT/X (GDAL/OGR, pilote CityJSON) | `planned_gdal` | niche |  |
| DICOM (Digital Imaging and Communications in Medicine) | Readers/Writers |  | n/a |  | `out_of_scope` | niche | Format d'imagerie médicale (radiologie, pathologie) — hors du domaine géospatial couvert par GeoStudio malgré un pilote GDAL existant pour ce format à d'autres fins. |
| ER Mapper ECW | Readers/Writers |  | gdal | SDK Hexagon ERDAS ECW/JP2 (propriétaire) requis pour l'écriture et pour une lecture complète — licence commerciale, decodeur gratuit limité | `license_blocked` | niche |  |
| ERDAS IMAGINE (.img) | Readers/Writers |  | gdal | MIT/X (GDAL, pilote HFA — spécification ouverte, aucune SDK requise) | `planned_gdal` | niche |  |
| Entwine Point Tile (EPT) | Readers/Writers |  | pdal | BSD-3-Clause (PDAL, pilote natif EPT) | `planned_pdal` | niche |  |
| Esri File Geodatabase (FileGDB) | Readers/Writers |  | gdal | MIT/X (GDAL/OGR, pilote OpenFileGDB — lecture et écriture partielle sans SDK Esri) | `planned_gdal` | courant | Le pilote OpenFileGDB de GDAL est indépendant du SDK propriétaire Esri FileGDB API — écriture complète plus limitée que la lecture, mais aucun blocage de licence pour un usage basique. |
| Esri Geodatabase (ArcSDE, entreprise) | Readers/Writers |  | gdal | Nécessite le SDK propriétaire Esri ArcSDE/ArcObjects (licence commerciale ArcGIS) pour un accès complet — pas d'implémentation ouverte équivalente connue | `license_blocked` | niche | À distinguer du FileGDB de bureau (ligne séparée), qui lui n'exige aucune SDK Esri. |
| Esri Shapefile | Readers/Writers |  | gdal | MIT/X (GDAL/OGR) | `planned_gdal` | courant | Le module d'ingestion GeoStudio importe déjà le Shapefile (core/app/ingestion, branche zip) — mais aucun op de pipeline no-code n'expose ce format en lecture/écriture ; catégorisé contre ops_catalog()/QGIS uniquement, pas contre l'ingestion (cf. consigne de la tâche). |
| Extensis MrSID | Readers/Writers |  | gdal | SDK LizardTech : décodage gratuit mais encodage (écriture) sous licence commerciale — propriétaire pour un usage complet lecture+écriture | `license_blocked` | niche |  |
| GML (Geography Markup Language) | Readers/Writers |  | gdal | MIT/X (GDAL/OGR, pilote GML) | `planned_gdal` | courant | Ingestion GeoStudio traite déjà GML comme KML (driver GDAL, cf. GAP-29) ; pas d'op pipeline équivalent pour un GML arbitraire en entrée/sortie de pipeline. |
| GeoJSON | Readers/Writers |  | gdal | MIT/X (GDAL/OGR) | `planned_gdal` | courant | Ingestion GeoStudio couvre déjà l'import GeoJSON (branche geojson/json) ; aucun op pipeline reader.* générique de fichier ne l'expose aujourd'hui. |
| GeoTIFF | Readers/Writers |  | gdal | MIT/X (GDAL/OGR, pilote GTiff) | `planned_gdal` | courant |  |
| Google BigQuery | Readers/Writers |  | duckdb | MIT (DuckDB) + Apache-2.0 (dlt sql_database source, dialecte SQLAlchemy) | `planned_duckdb` | courant | Même architecture que reader.connector.snowflake (dialecte SQLAlchemy résolu par entry point, sqlalchemy-bigquery) — nouvel op reader.connector.bigquery à ajouter au catalogue, aucun nouveau moteur d'exécution. |
| Google Sheets | Readers/Writers |  | duckdb | MIT (DuckDB) + Apache-2.0 (dlt, source Google Sheets vérifiée) | `planned_duckdb` | niche | Source attributaire par API plutôt que fichier — même famille architecturale que les futurs connecteurs SQL (dlt → DuckDB), pas un simple fichier statique. |
| JPEG 2000 (GeoJP2/GMLJP2) | Readers/Writers |  | gdal | MIT/X (GDAL, pilotes JP2OpenJPEG/JP2ECW selon build — variante OpenJPEG BSD-2-Clause) | `planned_gdal` | niche | Le pilote basé sur la bibliothèque libre OpenJPEG (BSD-2-Clause) suffit ; ne pas confondre avec le pilote JP2ECW d'Hexagon (propriétaire, cf. ER Mapper ECW). |
| JSON (JavaScript Object Notation) | Readers/Writers |  | duckdb | MIT (DuckDB, extension json) | `planned_duckdb` | courant | Ingestion GeoStudio traite le JSON Lines (GAP-29, parse_jsonlines) ; le JSON générique (document unique ou tableau) n'a pas d'op pipeline dédié. |
| LDAP/Active Directory | Readers/Writers |  | n/a |  | `out_of_scope` | niche | Service d'annuaire d'identités, sans rapport avec le domaine géospatial/analytique — l'identité de GeoStudio passe par OIDC/Keycloak, jamais par un accès direct LDAP en pipeline. |
| Landsat-8 | Readers/Writers |  | gdal | MIT/X (GDAL, pilote raster générique + métadonnées MTL) | `planned_gdal` | niche |  |
| MapInfo MIF/MID | Readers/Writers |  | gdal | MIT/X (GDAL/OGR) | `planned_gdal` | niche | Même famille de pilote GDAL que MapInfo TAB. |
| Microsoft Excel | Readers/Writers |  | duckdb | MIT (DuckDB, extension spatial/excel) | `planned_duckdb` | courant | Ingestion GeoStudio importe déjà l'XLSX géoréférencé (SP-56, parse_xlsx_latlon) et le multi-feuilles (GAP-29, list_xlsx_sheets) — aucun op de pipeline n'expose un classeur Excel arbitraire en lecture/écriture pour un usage attributaire hors ingestion. |
| Microsoft PowerPoint | Readers/Writers |  | n/a |  | `out_of_scope` | niche | Format de présentation bureautique, sans rapport avec le pipeline de données — GeoStudio a son propre mécanisme de rapport PDF (ReportSchedule, SP-17), pas un besoin PowerPoint. |
| Microsoft SQL Server Spatial (JDBC) | Readers/Writers |  | duckdb | MIT (DuckDB) + Apache-2.0 (dlt sql_database source, dialecte pyodbc/pymssql) | `planned_duckdb` | courant | Même patron que postgres/snowflake : nouvel op reader.connector.mssql, dialecte SQLAlchemy. |
| MongoDB | Readers/Writers |  | duckdb | MIT (DuckDB) + Apache-2.0 (dlt, source MongoDB vérifiée) | `planned_duckdb` | niche | Jugement limite : MongoDB est un magasin de documents, pas relationnel/SQL au sens strict — classé ici car dlt fournit déjà une source MongoDB vérifiée matérialisant vers DuckDB, même architecture connecteur que postgres/snowflake malgré la nature non tabulaire de la source. |
| NetCDF (Network Common Data Form) | Readers/Writers |  | gdal | MIT/X (GDAL, pilote netCDF, dépend de libnetcdf sous licence BSD-like) | `planned_gdal` | niche |  |
| OGC GeoPackage | Readers/Writers |  | gdal | MIT/X (GDAL/OGR) | `planned_gdal` | courant | Ingestion GeoStudio couvre déjà le GeoPackage (branche gpkg) ; pas d'op pipeline équivalent. |
| OGC WFS (Web Feature Service) | Readers/Writers |  | gdal | MIT/X (GDAL/OGR, pilote WFS) | `planned_gdal` | courant | GeoStudio a par ailleurs un connecteur de moissonnage WFS/WMS/WMTS côté fédération (app/harvest, SP-12) — module distinct de reader.* pipeline, hors périmètre de comparaison de cette tâche. |
| OGC/Google KML | Readers/Writers |  | gdal | MIT/X (GDAL/OGR, pilote LIBKML) | `planned_gdal` | courant | Ingestion GeoStudio importe déjà KML/KMZ (SP-56, parse_kml) ; aucun op pipeline reader.*/writer.* ne l'expose — bon exemple de la distinction ingestion ≠ op de pipeline demandée par cette tâche. |
| OpenStreetMap (OSM) XML / PBF | Readers/Writers |  | gdal | MIT/X (GDAL/OGR, pilote OSM) | `planned_gdal` | courant |  |
| Oracle Spatial Relational | Readers/Writers |  | duckdb | MIT (DuckDB) + Apache-2.0 (dlt) + Apache-2.0 (python-oracledb, mode thin sans Instant Client) | `planned_duckdb` | niche | python-oracledb en mode thin (pur Python, Apache-2.0) évite la dépendance à l'Instant Client Oracle et sa licence OTN à part — pas de blocage de licence identifié pour un futur connecteur. |
| PostGIS | Readers/Writers | reader.connector.postgres | duckdb | MIT (DuckDB) + Apache-2.0 (dlt) | `implemented` | courant | Même op que PostgreSQL Non-Spatial — la géométrie transite en texte (WKT/WKB hexadécimal) via la requête SQL, pas en tant que type géométrique natif décodé automatiquement ; jugement assumé, à vérifier si un besoin de géométrie native sur ce connecteur émerge. |
| PostgreSQL Non-Spatial (JDBC) | Readers/Writers | reader.connector.postgres | duckdb | MIT (DuckDB) + Apache-2.0 (dlt) | `implemented` | courant | Correspond à l'op reader.connector.postgres (dlt → destination DuckDB), déjà livré SP-15f — requête SQL arbitraire sur une base Postgres externe au tenant, distincte de reader.collection (collections propres à GeoStudio). |
| Precisely MapInfo TAB (MITAB) | Readers/Writers |  | gdal | MIT/X (GDAL/OGR, pilote MITAB) | `planned_gdal` | niche | Pilote MITAB de GDAL entièrement open source (lecture et écriture), aucune SDK propriétaire requise. |
| RIEGL Laser Scan Database (RDB) | Readers/Writers |  | pdal | SDK propriétaire RIEGL rdblib requis (licence constructeur) — pas d'implémentation ouverte connue pour lire le format RDB natif | `license_blocked` | niche |  |
| SAP Sybase Adaptive Server Enterprise (ASE) | Readers/Writers |  | n/a |  | `out_of_scope` | niche | Base de données métier legacy d'entreprise, exemple typique cité par la méthode de catégorisation (« bases propriétaires métier ») — aucun cas d'usage géospatial identifié. |
| Salesforce | Readers/Writers |  | n/a |  | `out_of_scope` | niche | Connecteur métier CRM sans rapport avec un pipeline de données géospatiales/analytiques. |
| Sentinel-2 MSI SAFE Raster | Readers/Writers |  | gdal | MIT/X (GDAL, pilote SENTINEL2) | `planned_gdal` | niche | Lecture de format brut = I/O GDAL ; un futur traitement avancé (indices spectraux, correction radiométrique) relèverait d'OTB, mais cette ligne ne catégorise que la lecture du conteneur SAFE. |
| Snowflake Non-Spatial | Readers/Writers | reader.connector.snowflake | duckdb | MIT (DuckDB) + Apache-2.0 (dlt) + Apache-2.0 (snowflake-sqlalchemy) | `implemented` | courant | Op livré par le chantier GAP-16 (cf. CLAUDE.md ### Livré) — round-trip réel non câblé en CI (pas d'émulateur Snowflake auto-hébergeable), test marqué @pytest.mark.snowflake manuel. |
| TopoJSON | Readers/Writers |  | n/a |  | `unknown` | niche | Aucun pilote GDAL/OGR TopoJSON confirmé (contrairement à GeoJSON) et aucune bibliothèque Rust mature identifiée en recherche limitée à cette tâche — à retrancher lors d'un futur passage dédié plutôt que deviner un moteur candidat. |
| WCS (Web Coverage Service) | Readers/Writers |  | gdal | MIT/X (GDAL, pilote WCS) | `planned_gdal` | niche |  |
| WMS (Web Map Service) | Readers/Writers |  | gdal | MIT/X (GDAL/OGR, pilote WMS) | `planned_gdal` | courant | GeoStudio a un connecteur de moissonnage WMS/WMTS distinct côté fédération (app/harvest, SP-12) — hors périmètre de comparaison de cette tâche (pipeline ops uniquement). |
| World Meteorological Organization GRIB | Readers/Writers |  | gdal | MIT/X (GDAL, pilote GRIB) | `planned_gdal` | niche |  |
| AttributeExposer | Schema/Feature Type | transform.exposeAttributes | duckdb |  | `implemented` | niche | Nécessite de transformer une valeur en plusieurs colonnes nommées dynamiquement selon le contenu de la liste — au-delà d'une expression scalaire à une colonne de sortie (transform.derive), même limite que ListExploder pour l'explosion en lignes. |
| FeatureJoiner | Schema/Feature Type | transform.join | duckdb |  | `implemented` | courant | Correspondance directe avec la sémantique de transform.join (jointure inner/left par clé). |
| FeatureMerger | Schema/Feature Type | transform.mergeChildren | duckdb |  | `implemented` | courant | Produit une liste de structures imbriquées par groupe (parent + toutes ses lignes enfant complètes), pas une métrique scalaire — au-delà du dict de métriques agrégées scalaires de transform.aggregate (bien que DuckDB supporte list(struct_pack(...)) nativement, ce n'est pas exposé par le contrat actuel de l'op). |
| FeatureTypeExtractor | Schema/Feature Type | transform.derive | duckdb |  | `implemented` | niche | Un transform.derive posant une valeur littérale (le nom de la collection source, connu à l'écriture du pipeline) sur chaque branche AVANT une éventuelle fusion (transform.merge) reproduit ce marquage — chaque collection étant déjà un nœud reader.collection distinct dans GeoStudio, contrairement au flux multi-type unique de FME. |
| FeatureTypeFilter | Schema/Feature Type |  | duckdb |  | `planned_duckdb` | niche | Le modèle de pipeline GeoStudio sépare déjà chaque source en un nœud reader.collection homogène dédié — le problème que ce transformer résout (démêler un flux hétérogène unique) ne se pose pas de la même façon aujourd'hui ; conservé en moteur candidat pour le jour où un reader ingérerait un flux réellement mixte (ex. JSON hétérogène). |
| ListBuilder | Schema/Feature Type | transform.derive | duckdb |  | `implemented` | niche | list_value(a, b, c) — fonction de construction de liste confirmée dans la documentation DuckDB, expression bornée à une ligne produisant une valeur de type LIST. Non vérifié : si writer.collection peut effectivement persister la colonne LIST résultante — core/app/collections/introspection.py::FieldType n'a aujourd'hui aucun type liste/tableau. Le calcul lui-même fonctionne via transform.derive, mais l'écriture du résultat dans une collection pourrait ne pas être supportée. |
| ListExploder | Schema/Feature Type | transform.explodeList | duckdb |  | `implemented` | niche | Multiplication du nombre de lignes en sortie (UNNEST) — app.pipelines.expr_validation interdit explicitement toute référence de table dans une expression bornée transform.derive, ce qui exclut par construction toute opération multipliant les lignes. |
| SchemaMapper | Schema/Feature Type | transform.mapSchema | duckdb |  | `implemented` | courant | Nécessite une introspection dynamique du schéma cible ET source au moment de l'exécution — au-delà du dict statique colonne→colonne connu à l'écriture du pipeline (transform.select). |
| SchemaScanner | Schema/Feature Type | transform.scanSchema | duckdb |  | `implemented` | niche | Opération de méta-introspection sur l'ensemble du jeu de données, pas une transformation ligne à ligne — aucun op pipeline actuel n'expose l'inférence de schéma agrégé. |
| AngleConverter | String/Text | transform.derive | duckdb |  | `implemented` | niche | Arithmétique simple avec la fonction pi() (standard SQL, présente dans DuckDB) — col * pi() / 180 pour degrés→radians, expression bornée. |
| BaseConverter | String/Text | transform.derive | duckdb |  | `implemented` | niche | to_base(nombre, radix) — fonction de conversion de base confirmée dans la documentation DuckDB, avec to_hex/hex/bin/unhex/unbin en complément selon la base cible. |
| BinaryDecoder | String/Text | transform.derive | duckdb |  | `implemented` | niche | from_base64(chaîne)/unhex(chaîne) — fonctions confirmées dans la documentation DuckDB. |
| BinaryEncoder | String/Text | transform.derive | duckdb |  | `implemented` | niche | to_base64(blob)/hex(blob) — fonctions confirmées dans la documentation DuckDB. |
| CharacterCodeExtractor | String/Text | transform.derive | duckdb |  | `implemented` | niche | ASCII(col) — fonction confirmée dans la documentation DuckDB, retourne le point de code du premier caractère. |
| CharacterCodeReplacer | String/Text | transform.derive | duckdb |  | `implemented` | niche | CHR(code) pour reconstruire un caractère depuis son code, combiné à REPLACE/REGEXP_REPLACE — expression bornée composée de fonctions confirmées côté DuckDB. |
| DateTimeCalculator | String/Text | transform.derive | duckdb |  | `implemented` | courant | date_diff(unité, début, fin) ou arithmétique d'intervalle (date + INTERVAL n unité) — fonctions confirmées dans la documentation DuckDB. |
| DateTimeConverter | String/Text | transform.derive | duckdb |  | `implemented` | courant | strftime(date, gabarit)/strptime(chaîne, gabarit) — fonctions de formatage de date confirmées dans la documentation DuckDB. |
| DateTimeRounder | String/Text | transform.derive | duckdb |  | `implemented` | niche | date_trunc(précision, date) — fonction de troncature de date confirmée dans la documentation DuckDB. |
| DateTimeStamper | String/Text | transform.derive | duckdb |  | `implemented` | niche | current_timestamp/now() — expression SQL bornée, non déterministe d'une exécution à l'autre par nature (cas d'usage attendu d'un horodatage). |
| ExpressionEvaluator | String/Text | transform.derive | duckdb |  | `implemented` | courant | Correspondance conceptuelle directe : transform.derive EST un évaluateur d'expression SQL bornée par ligne — le même mécanisme qu'ExpressionEvaluator, dans un langage différent (SQL DuckDB plutôt que le langage d'expression FME). |
| FilenamePartExtractor | String/Text | transform.derive | duckdb |  | `implemented` | niche | REGEXP_EXTRACT(col, motif, groupe) — fonction confirmée dans la documentation DuckDB, permet d'extraire chaque composant par un motif dédié. |
| HTMLExtractor | String/Text |  | duckdb |  | `planned_duckdb` | niche | Une extraction HTML robuste nécessite un vrai parseur DOM/sélecteur CSS ; une approximation par expression régulière bornée (REGEXP_EXTRACT) est fragile sur du HTML réel mal formé — retenu en moteur candidat futur plutôt que couvert aujourd'hui. |
| HTMLToXHTMLConverter | String/Text |  | n/a |  | `out_of_scope` | niche | Utilitaire générique de mise en forme de document, sans rapport avec le modèle de données géospatial de GeoStudio (aucune donnée du cœur ne transite par du HTML/XHTML comme format d'échange). |
| NLPClassifier | String/Text |  | n/a |  | `unknown` | niche | Nécessiterait une capacité de classification de texte (modèle ML) totalement absente de GeoStudio aujourd'hui ; recherche insuffisante en session pour recommander un moteur précis (bibliothèque Rust de classification légère vs service externe). |
| NLPTrainer | String/Text |  | n/a |  | `unknown` | niche | Même limite que NLPClassifier : entraînement de modèle ML, hors périmètre de recherche de cette session. |
| RandomNumberGenerator | String/Text | transform.derive | duckdb |  | `implemented` | niche | random() — fonction SQL scalaire standard de génération de nombre pseudo-aléatoire, présente dans tous les moteurs SQL courants dont DuckDB. |
| StringCaseChanger | String/Text | transform.derive | duckdb |  | `implemented` | courant | UPPER(col)/LOWER(col) — fonctions SQL scalaires standard, vérifiées dans la documentation DuckDB réelle (duckdb.org/docs/stable/sql/functions/text.md). |
| StringConcatenator | String/Text | transform.derive | duckdb |  | `implemented` | courant | CONCAT(a, '-', b) ou opérateur \|\| — expression bornée à une ligne, fonction confirmée dans la documentation DuckDB. |
| StringFormatter | String/Text | transform.derive | duckdb |  | `implemented` | courant | format(gabarit, a, b, ...) ou printf(...) — fonctions de formatage façon fmt/printf documentées côté DuckDB, correspondance directe avec le besoin du transformer. |
| StringLengthCalculator | String/Text | transform.derive | duckdb |  | `implemented` | courant | LENGTH(col) — fonction SQL scalaire élémentaire, confirmée dans la documentation DuckDB. |
| StringPadder | String/Text | transform.derive | duckdb |  | `implemented` | niche | LPAD(col, n, car)/RPAD(col, n, car) — fonctions confirmées dans la documentation DuckDB (duckdb.org/docs/stable/sql/functions/text.md). |
| StringPairReplacer | String/Text | transform.derive | duckdb |  | `implemented` | niche | CASE WHEN col_a ~ motif THEN col_b ELSE col_a END — cas particulier de correspondance conditionnelle, expression bornée à une ligne. |
| StringReplacer | String/Text | transform.derive | duckdb |  | `implemented` | courant | REPLACE(col, 'a', 'b') pour un remplacement littéral, REGEXP_REPLACE(col, motif, remplacement) pour un motif — les deux fonctions sont documentées côté DuckDB (vérifié sur duckdb.org/docs). |
| StringSearcher | String/Text | transform.filter | duckdb |  | `implemented` | courant | REGEXP_MATCHES(col, motif) ou opérateur LIKE — test booléen bornée à une ligne, confirmé dans la documentation DuckDB ; même patron que AttributeFilter pour le routage multi-motif (plusieurs nœuds transform.filter). |
| SubstringExtractor | String/Text | transform.derive | duckdb |  | `implemented` | courant | SUBSTRING(col, début, longueur) — fonction SQL scalaire standard, confirmée dans la documentation DuckDB. |
| TextDecoder | String/Text |  | rust:encoding_rs | MIT/Apache-2.0 (encoding_rs) | `planned_rust` | niche | Même limite et même candidat moteur que TextEncoder (opération inverse). |
| TextEncoder | String/Text |  | rust:encoding_rs | MIT/Apache-2.0 (encoding_rs) | `planned_rust` | niche | Les fonctions encode()/decode() natives de DuckDB ne couvrent que l'UTF-8 (confirmé dans la documentation DuckDB) ; un jeu de caractères cible arbitraire nécessite une bibliothèque dédiée — encoding_rs (crate Rust mature, utilisée par le moteur de rendu Firefox) est un candidat naturel. |
| EnvironmentVariableFetcher | Workflow / Logic |  | n/a |  | `out_of_scope` | niche | Exposer les variables d'environnement du conteneur/hôte à un pipeline no-code auteuré par un tenant serait une violation de cloisonnement multi-tenant — même posture produit que le coffre de secrets AES-GCM dédié (SP-15e) qui évite précisément d'exposer des valeurs serveur brutes à l'utilisateur final. |
| FeatureHolder | Workflow / Logic |  | n/a |  | `out_of_scope` | niche | Sans objet dans le modèle d'exécution de GeoStudio : chaque nœud transform est compilé en une TEMP VIEW SQL exécutée sur un résultat déjà entièrement matérialisé par le nœud précédent (core/app/pipelines/compiler.py) — contrairement au moteur de streaming feature-par-feature de FME, il n'existe ici aucune notion d'arrivée partielle contre laquelle se prémunir. Rien à combler. |
| HTTPCaller | Workflow / Logic |  | duckdb |  | `planned_duckdb` | courant | reader.connector.rest (dlt) ne lit qu'une fois en tête de pipeline via une garde d'egress SSRF vérifiée au démarrage — un appel HTTP par ligne au milieu du pipeline (comme HTTPCaller) poserait une garde d'egress structurellement différente (un budget/débit par ligne plutôt qu'un contrôle unique sur l'URL de lecture), pas seulement une nouvelle capacité de connecteur. Techniquement rattachable au patron connector_runtime.py existant (dlt/Python au-dessus du DAG DuckDB), engine=duckdb par cohérence avec les autres readers/writers connecteurs de ce dépôt. |
| Inspector | Workflow / Logic |  | n/a |  | `out_of_scope` | courant | Outil de débogage propre au bureau FME Workbench en mode interactif — GeoStudio a son propre équivalent d'auteurage (canvas DAG + panneau d'exécution du builder de pipeline, SP-15), pas un op du pipeline lui-même. |
| Junction | Workflow / Logic | transform.merge | duckdb |  | `implemented` | courant | Pour 2 entrées (cas dominant), transform.merge (UNION ALL BY NAME, core/app/pipelines/ops/schemas.py::TransformMergeParams) reproduit exactement la sémantique de pur pass-through/fusion de Junction. Pour N>2 entrées, s'obtient en chaînant N-1 nœuds transform.merge successifs — une simple composition linéaire, aucune limite de topologie (chaque merge garde un seul prédécesseur primaire + un secondaire). |
| Logger | Workflow / Logic |  | n/a |  | `out_of_scope` | niche | Outil d'aide au débogage pendant l'auteurage interactif dans FME Workbench — un pipeline GeoStudio est un DAG déclaratif exécuté de façon headless (file procrastinate), sans notion de journal de traduction utilisateur par nœud à ce niveau. Observabilité déjà couverte par ailleurs (OTel, SP-10). |
| ParameterFetcher | Workflow / Logic |  | duckdb |  | `planned_duckdb` | niche | Rapproché des « bindings CEL généralisés + variables typées » déjà au périmètre SP-5 (CLAUDE.md, décisions figées) — exposer une valeur de paramètre/variable typée de pipeline comme colonne calculée serait une extension naturelle de transform.derive une fois ce mécanisme de binding disponible pour les pipelines, pas une nouvelle catégorie de moteur. |
| Player | Workflow / Logic |  | n/a |  | `out_of_scope` | niche | Symétrique de Recorder, même rationale : aide à l'auteurage itératif de FME Workbench, sans équivalent dans le modèle d'exécution headless de GeoStudio. |
| PythonCaller | Workflow / Logic |  | duckdb |  | `planned_duckdb` | courant | Aucun des 19 op actuels n'exécute de code utilisateur arbitraire (transform.filter/derive/aggregate n'acceptent que des expressions SQL DuckDB bornées, jamais du code Python — cf. avertissement en tête de core/app/pipelines/ops/schemas.py). DuckDB expose une vraie API Python UDF (con.create_function, vérifiée sur duckdb.org/docs/current/clients/python/function) — un futur op transform.python serait bâti dessus, mais poserait un vrai défi de bac à sable/sécurité (exécution de code utilisateur arbitraire côté serveur) au-delà d'un simple ajout de paramètre, à traiter comme un sous-projet à part entière plutôt qu'une extension mineure d'un op existant. |
| Recorder | Workflow / Logic |  | n/a |  | `out_of_scope` | niche | Fonctionnalité de productivité d'auteurage itératif propre à FME Workbench (mise en cache d'un flux intermédiaire pendant le développement d'un workspace) — sans objet pour un pipeline GeoStudio compilé et exécuté comme un DAG SQL complet à chaque run, pas de notion de session d'auteurage à mettre en cache. |
| Sorter | Workflow / Logic | transform.sort | duckdb |  | `implemented` | courant | DuckDB supporte nativement ORDER BY sur une ou plusieurs colonnes — aucun des 19 ops actuels n'expose de tri explicite (transform.select ne fait que sélectionner/renommer des colonnes). Un futur op transform.sort (liste de colonnes + directions) serait une addition triviale côté compilateur SQL, sans nouveau moteur. |
| SpatialSorter | Workflow / Logic | transform.sort | duckdb |  | `implemented` | niche | DuckDB spatial expose bien ST_Hilbert(geometry, bounds) (encode le centre de la boîte englobante d'une géométrie en index de courbe de Hilbert) — ORDER BY ST_Hilbert(...) est l'idiome documenté par DuckDB lui-même pour ce besoin exact, aucun calcul dédié à écrire. Le blocage réel n'est pas l'absence de fonction mais l'absence d'un op de tri générique dans le catalogue (pas de transform.sort aujourd'hui) — planned_duckdb reste correct, mais pour cette raison-là, pas pour une lacune de DuckDB spatial. |
| Terminator | Workflow / Logic |  | duckdb |  | `planned_duckdb` | niche | Aucun op d'assertion explicite parmi les 19 ops actuels (une erreur SQL fait déjà échouer tout le run, mais rien ne permet à l'auteur du pipeline de déclarer sa propre condition d'échec avec message personnalisé). Un futur op type transform.assert (expression bornée à la transform.filter, échec du run si elle s'évalue à faux sur au moins une ligne) couvrirait ce besoin sans changer de moteur. |
| VariableRetriever | Workflow / Logic |  | n/a |  | `out_of_scope` | niche | Symétrique de VariableSetter, même incompatibilité architecturale (état mutable global) — hors périmètre pour la même raison. |
| VariableSetter | Workflow / Logic |  | n/a |  | `out_of_scope` | niche | Introduit un état mutable global lu/écrit à des points arbitraires et non connectés du workflow — contredit la règle d'architecture n°2 de ce dépôt (« pas de logique cachée hors config », CLAUDE.md) et le modèle de compilation DAG→SQL déclaratif et sans effet de bord de core/app/pipelines/compiler.py. Les bindings CEL généralisés + variables typées déjà au périmètre SP-5 couvrent un besoin voisin mais explicitement scopé aux configs déclaratives, pas à un état mutable au fil de l'exécution. |
| WorkspaceRunner | Workflow / Logic |  | n/a |  | `out_of_scope` | niche | Pas un manque de couverture moteur (DuckDB/GDAL/etc.) mais une capacité d'orchestration absente de l'architecture : aucun pipeline GeoStudio ne peut aujourd'hui invoquer un autre pipeline comme sous-étape (core/app/pipelines/runtime.py compile et exécute un seul DAG à la fois). Nécessiterait une capacité d'orchestration de pipelines imbriqués, hors périmètre de la matrice de couverture moteur. |
